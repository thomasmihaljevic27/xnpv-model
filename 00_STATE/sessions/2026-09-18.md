# Session 2026-09-18 — the goalie comparison, corrected

Three findings on yesterday's goalie bake-off. All three are real, and the first reverses the
headline.

## 1. I compared production against a lookalike of production

I rebuilt production's goalie cascade from the code and called the result "production's rule". It
was not. Three mechanisms were missing:

- production's lookup table has **no games filter** — a two-game season is a prior like any other,
  where mine dropped anything under ten games;
- the cascade fills the t−1, t−2, t−3 slots **strictly**. A goaltender with no t−1 season does not
  fall back to a 60/40 of whatever else exists, which is what mine did; he goes to a **stale anchor**
  computed at an earlier standpoint, bounded three seasons back;
- that stale-anchor population shrinks toward **0.650**, not the league average.

The third I had never read. I read the first half of `shrunk_projection`, stopped at the cascade,
and described the whole method — which is the one thing CLAUDE.md names explicitly: *don't describe
a mechanism you haven't read the code for*. I had the rule in front of me and quoted half of it.

The gap is not cosmetic. The lookalike is **0.094 WAR worse** than the real projector overall, and
up to **2.28 WAR** apart on a single goaltender. Everything I concluded from beating it is gone.

The candidate now imports `GoalieProjector` and calls it — the pattern the qualifying-offer check
already used, which I had built two days ago and then did not apply here. The reimplementation
stays in the bake-off, labelled "a simplified cascade", because the gap between it and the real
thing is itself informative.

## 2. The bootstrap paired forecasts from different years

`paired_bootstrap` joined on goaltender and horizon and left out the page, so a 2015 forecast was
matched against a 2021 one for the same goaltender. 3,683 intended pairs became 19,853 rows, and
the weighting drifted toward goaltenders who appear on many pages. Workload weighting came out at
78% of resamples; correctly paired against the real benchmark, 0%.

A pairing bug is the same class of error as the shared random stream last week: a join key that is
one column short is a silent reweighting, not a crash.

## 3. The ageing candidate never used age

`np.polyfit(x, y, 1)[1]` is the **intercept**. I took it, called it the age slope, and applied one
common drift to every goaltender. Adding twenty years to every subject moved the forecast by
exactly zero — which the reviewer tested and I had not.

So the sign flip I reported and built a standing flag on was in the drift, not in ageing.

## Where the numbers land

| rule | MAE | bias | beats production |
|---|---:|---:|---:|
| league average | 1.634 | +0.092 | 0% |
| **production's own projector** | **1.488** | +0.101 | — |
| a simplified cascade (the old "production") | 1.582 | +0.218 | 0% |
| same on the page's own average | 1.511 | +0.045 | 1% |
| same, kept weight fitted (0.43) | 1.491 | **+0.036** | 34% |
| shrunk by games behind | 1.585 | +0.164 | 0% |
| with a fitted age slope | 1.552 | +0.034 | 0% |

**Production's projector wins, and my improvement claim is withdrawn.** The closest candidate is
0.003 WAR worse and takes a third of resamples: a tie.

**The bias result survives and is worth keeping separate.** Production runs +0.101 high at every
horizon; the fitted-weight rule runs +0.036 at the same error. For a forecast that gets multiplied
by a price line and summed over eight seasons, a standing bias is a different kind of defect from
noise — noise averages out over a contract and a roster, a bias does not. That goes into the
price-line decision as a known defect rather than a reason to replace the rule.

**On ageing, the corrected fit says something stronger than the withdrawn claim.** Two coefficients,
and only one is about age:

| page | per year of age | common drift |
|---|---:|---:|
| 2015 | −0.0479 | +0.1167 |
| 2017 | −0.0260 | +0.0792 |
| 2019 | −0.0711 | −0.0019 |
| 2021 | −0.0722 | −0.1057 |

The age slope is **negative on every page**. The drift is what swings. So there is no basis for
"ageing is unidentified here", and the flag is rewritten. Using the slope still does not beat
production (+0.065, 0%), which is a statement about this implementation on 82 seasons a year — not
about whether goaltenders age. The selection concern stands and is untested.

## The pattern in these three

All three are the same shape: a comparison that looked clean because the thing being compared was
not what the label said. A benchmark that was a lookalike, a pairing that matched forecasts across
years, a coefficient that was not the one named. None of my thirty checks could see any of it,
because each tested that the machinery ran, not that it was pointed at the right thing.

The four new checks all test identity rather than behaviour: the benchmark **is** production's
class, the two rules **differ**, one forecast **is** one (page, goaltender, horizon), and a
candidate that says it uses age **moves** when age moves.

## Suite

**32 passed, 0 skipped, 0 failed.** Four reintroduced defects caught: a lookalike in production's
place, the two rules coinciding, the pairing dropping the page, and the age term reverted to the
intercept.

## Files touched

    50_REBUILD/code/run_goalie_bakeoff.py    v2.0
    50_REBUILD/code/repair_checks.py         checks 31-32
    50_REBUILD/docs/Goalie_Bakeoff.md        rewritten

Nothing adopted. The forecast to carry into the price line is production's own projector, with its
+0.101 bias recorded. Next in the branch: the goalie price line, then participation, then the
control-year gate.

---

# Session 2026-09-18b — the bias qualified, and the goalie price line

## The qualification, folded in

The review closed with one thing to fix: I had called production's +0.101 WAR mean error a defect
for the price line to correct. It is not established, for two reasons I checked myself.

Resampling whole careers puts production's mean error between **−0.129 and +0.315**. The interval
contains zero.

And the bias is not only the forecast's. Every candidate shares one participation estimator by
design, and that estimator says **64.1%** of these goaltender-seasons are played where **55.3%**
were. At 1.89 WAR for a season actually played, that gap alone would produce **+0.167 WAR** — more
than the whole observed bias. Every candidate carries it.

So the pooled mean error cannot be attributed to anybody's ability forecast. Only the *difference*
between two candidates' biases is attributable, and **nothing downstream may move a dollar price to
cancel a pooled error**. The runner now computes both of those numbers and prints them beside the
bias, so the qualification travels with the figure instead of living in a review.

That is a rule worth keeping generally: a pooled error is a sum of everything in the chain, and
attributing it to one link needs the other links measured first.

## Is a goalie win priced like a skater win?

The branch's second step, and the question that decides whether goaltenders can sit on the project's
single scale at all. One censored line over skaters and goaltenders together, a goaltender indicator
and an interaction, refitted at each signing quarter on contracts signed before it. The interaction
**is** the difference in dollars per forecast win.

| fitted on contracts signed before | n | $/skater win | $/goalie win | ratio |
|---|---:|---:|---:|---:|
| 2018-07 | 365 | 0.212 | 0.264 | 1.25 |
| 2019-10 | 947 | 1.316 | 1.321 | 1.00 |
| 2021-01 | 1,270 | 0.899 | 1.047 | 1.16 |
| 2022-01 | 1,657 | 0.820 | 0.965 | 1.18 |

**A goaltender's forecast win prices at $0.96M against a skater's $0.82M** on the last fit. The
window runs 0.74 to 1.57, and the spread is almost entirely in the early fits — under about 600
contracts the interaction is not pinned down; from 900 on it sits between 0.87 and 1.22.

I have written it as a conditional association and not as the price of a win, because it is one.
Nobody randomised who got which contract. And the two forecasts are not on the same footing: the
skater side is the rebuild's leader with an aging path, the goalie side is production's flat,
heavily-shrunk projector on 82 seasons a year. Part of any slope difference is that difference, and
anyone reading this as "clubs underpay goaltenders" is reading it wrong.

## The structural answer

| line | goalie contracts priced | mean abs error (cap share) | bias |
|---|---:|---:|---:|
| one line, no goaltender terms | 174 | 0.0103 | +0.0066 |
| one line **with** goaltender terms | 174 | **0.0075** | +0.0001 |
| a goalie-only line | 5 | — | never fittable |

Adding the two goaltender terms cuts held-out error by more than a quarter and takes the bias to
nothing. A goalie-only line needs 200 contracts signed before each decision and the development
sample holds 266 in total, so it first becomes fittable in the last quarter of the window and prices
five contracts. That is unavailable, not unattractive, and the runner says so rather than letting a
five-contract error look like a win.

**For goaltenders the answer is one market with two terms** — not one line for everybody, and not a
separate market. That settles the open part of D7 for this position group on development evidence.

## Housekeeping

`contract_price_model.contract_sample` now takes a position group rather than assuming skaters, so
both samples come off one census with one set of rules for the cap-share denominator, the
league-minimum floor and the signing date. One census, one rule, two samples — rather than a second
copy of the census for goaltenders.

## Checks

**33 passed, 0 skipped, 0 failed.** The new one tests the arithmetic that the whole run reduces to:
on synthetic contracts built with a known goalie slope the fitted interaction comes back at 0.0049
against 0.0050 given, first-year production priced at nothing comes back at nothing, and the dollars
helper adds the interaction to the base slope rather than replacing it. The fit also refuses a
contract signed after the decision. Both verified by breaking them.

Building that check caught a mistake in my own test design first: I had made first-year production a
copy of the season average, which put two identical regressors in the line and split the slope
between them. The check failed for a reason that was about collinearity rather than about the fit.

## Files

    50_REBUILD/code/run_goalie_bakeoff.py      v2.1  (the bias diagnostic)
    50_REBUILD/code/run_goalie_price_line.py   new
    50_REBUILD/code/contract_price_model.py    contract_sample takes a position group
    50_REBUILD/code/repair_checks.py           check 33
    50_REBUILD/docs/Goalie_Price_Line.md       new
    50_REBUILD/docs/Goalie_Bakeoff.md          the bias qualified

Nothing adopted. Next: the goalie participation model — a goaltender's share of the schedule is a
depth-chart question, and both the forecast and this price line carry a flat survival rate in its
place. The control-year gate comes after that, because a goaltender's tender decision is a
depth-chart decision as much as a value one.
