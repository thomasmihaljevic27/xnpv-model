---
session: 2026-09-09
topic: Post-migration verification of the full pipeline, then the Craft retirement + 00_STATE restructure
commit: (this session — see the DECISIONS.md v3.1 and v3.2 change-log entries; SHAs land on push)
---

## Timeline

- [WORK] Ran `draft_pick_linkage.py` + `draft_yield_curve.py` on the desktop machine.
  Rebuilt curve reproduces **to the cent** (pick 1 $13.65M · 2 $8.33M · 3-5 $6.83M · 6-10 $5.75M
  · 11-20 $4.30M · 21-32 $3.20M · 33-50 $2.44M · 51-100 $1.54M · 101-150 $1.09M · 151-224 $0.61M),
  monotone, `draft_pick_outcomes.csv` 2,324 picks / 1,166 true-zero busts, shape check r=0.866
  vs Bacon NHLer probabilities. `draft_yield_curve.csv` on disk is now the rebuilt (Stage 3 rate)
  version, not the pre-rebuild shallow one. → closes verification-gap item 2.

- [WORK] Ran the full player chain: `skater_value_engine` → `skater_forward_projection` →
  `rfa_terminal_value` → `exit_hazard` → `contract_npv` → `contract_npv_panel`.
  - Skater side reproduces digit-for-digit: Stage 0a/0b guards PASS, 6,892 priced skater-seasons,
    2,591 skater contracts, `[1a]` k=0 max diff $0.00, Stage 4 retention 21.3%→27.0% buckets
    unmoved, pooled median NPV +0.29M / p10 −7.80, panel 6,939 rows.
  - Goalie side did NOT match the top-of-queue target of 318 goalie / 2,909 total: got
    **390 goalie / 2,981 total**, `[1b]` 776 rows / 13 divergences (< 19 assert).

- [WORK] Chose to verify the goalie engine before trusting the number. Re-ran
  `goalie_value_engine.py` → `goalie_value_spine_v2.csv` rebuilds **byte-identical**
  (MD5 `55c935dd47627517c0b32c30f4771e11`, parity gate $0.000284, λ=0.65).

- [CORRECTION] Diagnosis: the "2,909 / 318" figure was a 2026-07-05 number carried forward into
  the 2026-09-08 top-of-queue block. The 2026-07-29 goalie stale-anchor / flat-carry fix
  (documented in the v2.9 change log — which updated median/p10/panel but not the contract total)
  made ~72 more goalie contracts priceable. That is the entire difference. **Not a migration
  break** — all inputs byte-identical, `git status` clean throughout.

- [WORK] Edited `PROJECT_STATE.md` (5 edits, → v3.1): marked the migration-verification task DONE,
  corrected 2,909→2,981 in the two places it appeared, marked the rebuilt draft curve and the
  regenerate-downstream-spines items closed. Committed **5977e84**, pushed to `origin/main`.

- [DISCUSSION] How essential is Craft under the Claude Code + git setup. Conclusion: not
  essential; the git migration absorbed the two things Craft uniquely provided (versioned,
  diffable history + out-of-session readability). Craft has also been a recurring liability
  (unreachable in ≥5 sessions, drift causing documented errors).

- [DECISION] **Retire Craft as a canonical surface.** It may persist as Thomas's personal
  reading/scratch space, but Claude does not read it, reconcile against it, or sync to it.
  "How it works" explainers live in `40_DOCS/` (the nineteen-doc plan).

- [DECISION] **Split `PROJECT_STATE.md` into four `00_STATE/` files** — `PROJECT_STATE.md`
  (objective, model spec, build state, data/scripts inventory, regression results, per-pillar
  status, file-management protocol), `WORK_QUEUE.md`, `DECISIONS.md` (locked decisions + the
  state-file change log), `STANDING_FLAGS.md` (Karl's axes + open questions + resolution record).

- [DECISION] **Adopt a per-session log** at `00_STATE/sessions/YYYY-MM-DD[letter].md`. One entry
  per substantive beat (not literally per message), in order, tagged
  `[WORK] [DECISION] [CORRECTION] [DISCUSSION] [DEFERRED]`. Mandatory "Open threads / no
  follow-up" section. The `DECISIONS.md` change log stays the curated arc; the session files are
  the granular substrate to grep. Generated + committed in the same close-out commit as the
  state edits — the session is not done until it is in.

- [WORK] Executed the split (→ v3.2): `PROJECT_STATE.md` 691 → ~330 lines; created
  `WORK_QUEUE.md`, `DECISIONS.md`, `STANDING_FLAGS.md`; created `sessions/` and migrated the
  two pre-split write-ups (`2026-08-28.md`, `2026-09-08.md`) verbatim; retargeted the refresh
  protocol onto the session log + `git log` instead of project chats + Craft hubs; updated
  `CLAUDE.md`. Word-count conservation check: 24,978 → 25,473 across all `00_STATE` markdown
  (the +495 is new headers, stubs, comments, frontmatter — no content dropped).

- [DEFERRED] Reading surface (GitHub web vs Obsidian vs editor) — Thomas's call, explicitly not
  touched.

- [WORK] Closed the three open threads before starting the explainer docs:
  - **Stage 2 length-term null test** — found the script (`term_premium_test.py` v1.0), ran
    `--sample-only` (Stage 1 guards pass, all spec constants exact), then the full run against
    `10_SOURCE/nhl_gamelogs.sqlite` (has `gv_adjusted`, 8,294 rows). Decisive null confirmed:
    main spec GV-adj K=3 n=1,781, f = −0.0135, 95% CI [−0.0360, +0.0089] contains zero,
    value-side share 0.000; same at K=2/K=4. GV-raw robustness legs not produced (script needs
    re-pointing at the raw GV table) — logged as optional. Caught a transcription slip: the
    length-spec R² is 0.727279, not 0.727349 as earlier records had it.
  - **MANIFEST.csv** — rebuilt as a complete inventory (77 rows: 65 git-tracked with git blob
    SHAs + `channel=git`, 12 `channel=dropbox` for the `30_OUTPUT` set + game-log DB). Was a
    ~13-row 2026-07-30 handoff list before. Added the `channel` column; updated the
    file-management protocol's MANIFEST + audit subsections to match.
  - **Craft historical depth** — assessed, not actioned: `DECISIONS.md` was originally built
    from the Craft workspace and already mirrors its Decision Log (D1-D27 + review items + ~30
    change-log entries). Low risk of lost depth. Flagged for Thomas to veto if he knows of
    specific Craft-only content.

## Decisions made this session

1. Craft retired as a canonical surface (see DECISIONS.md and PROJECT_STATE "Precedence & layers").
2. `00_STATE/` split into four files + `sessions/` log.
3. Per-session log format adopted (this file is the first written to spec).

## Work done / artifacts

- Scripts run: `draft_pick_linkage.py`, `draft_yield_curve.py`, `skater_value_engine.py`,
  `skater_forward_projection.py`, `rfa_terminal_value.py`, `exit_hazard.py`, `contract_npv.py`,
  `contract_npv_panel.py`, `goalie_value_engine.py`, `term_premium_test.py` (Stage 2 null).
  All guards pass.
- `term_premium_test_{sample,results,diagnostics}.csv` + `_runlog.txt` written to `30_OUTPUT/`.
- `MANIFEST.csv` rebuilt as a complete 77-row inventory with a `channel` column.
- Regenerated (gitignored, in `30_OUTPUT/`): `draft_pick_linkage.csv`, `draft_pick_outcomes.csv`,
  `draft_yield_curve.csv`, `skater_value_spine.csv`, `contract_npv_spine.csv` (2,981 rows),
  `goalie_value_spine_v2.csv` (byte-identical), `contract_npv_panel.csv` (6,939 rows).
- Tracked: `00_STATE/PROJECT_STATE.md` (v3.1 then v3.2 restructure), new `WORK_QUEUE.md`,
  `DECISIONS.md`, `STANDING_FLAGS.md`, `sessions/{2026-08-28,2026-09-08,2026-09-09}.md`,
  `CLAUDE.md`, `MANIFEST.csv`.
- Commits: `5977e84` (v3.1 verification). The v3.2 restructure commits at session close.

## Open threads / no follow-up

- **Stage 2 length-term null test** — CLOSED this session (`term_premium_test.py`, decisive
  null, all spec constants reproduce). All Stage 1-5 verification items are now closed.
- **MANIFEST.csv** — CLOSED this session (complete 77-row inventory, `channel` column added).
- **Craft historical depth** — assessed as already captured in `DECISIONS.md`; not actioned.
  Open only in the sense that Thomas can veto if he knows of Craft-only content worth pulling in.
- **GV-raw robustness legs of the Stage 2 test** — new, minor. `term_premium_test.py` produces
  only the GV-adj headline unless re-pointed at the raw GV table (rebased, then zero-sum). The
  GV-adj null is unambiguous so this is an optional extension, not a gap.
- **`10_SOURCE/nhl_gamelogs.sqlite` row count** — `player_game_value` reads 427,169 rows here
  vs 427,241 in older records. Almost certainly the "VACUUM, newer is smaller" copy the v3.0
  migration flagged; the locked 4b results were computed against this same DB, so not chased.
- The `40_DOCS/` explainer set (now five docs, Doc 1 = Circularity and Game Value) is the next
  substantive work — deliberately left for a fresh session.

- [DISCUSSION] Reviewed the nineteen-document `Explainer_Document_Plan.docx` before starting to
  draft: nineteen was a delivery problem, not a content problem — Karl needs one file per
  question, not one per script. Proposed condensing to five or six documents grouped by
  narrative arc; Thomas set the final grouping.

- [DECISION] **Condense the explainer plan from nineteen documents to five**, same coverage
  (every component still states all four required elements — how, why, what it doesn't do,
  what's untested), fewer delivery units:
  1. Circularity and Game Value (unchanged, standalone — the one explicit ask).
  2. Player Pillar I — Data, Baseline, and Projection (data foundations, trailing baseline,
     aging curve, exit hazard).
  3. Player Pillar II — Pricing, Discounting, and Assembly (RFA terminal value, market rate,
     NPV assembly, goaltenders).
  4. Drafting, Prospects, and Unbuilt Components (draft yield curve + traded-pick pricing,
     both built and locked, labelled as such; prospects, retention pricing, trade clauses,
     and the back-test design, all designed but not built, labelled as such). Thomas's call
     specifically: fold the draft pillar in here rather than give it (or leave it out of) its
     own document — it's real, locked work, just thinner than the player pillar, so it sits
     with the unbuilt components with an explicit built/unbuilt label per section rather than
     getting its own file or being silently grouped with undone work.
  5. Cross-Cutting — Identification, Findings, Verification, and Bias (synthesis, written last,
     assembled from material the other four already contain).

  Considered and rejected: folding the draft pillar into "Unbuilt Components" unlabelled — flagged
  to Thomas because the draft pillar is built and locked (D22-D27), and an unlabelled merge would
  read to Karl as if it weren't. Resolved by keeping it in the same document but stating build
  status per section.

- [WORK] Rebuilt `40_DOCS/Explainer_Document_Plan.docx` to the five-document structure via
  docx-js (soffice couldn't render a PDF preview in this environment — it fails on even a
  trivial docx or a plain `.txt` conversion, an environment limitation, not a file defect;
  verified instead via XSD schema validation, which passed, plus a text-extraction pass
  confirming all nineteen original components and every "Must cover" / "Open / untested" bullet
  survived the merge under the new Doc/section numbering). Cross-references between components
  (e.g. "Doc 5" for exit hazard) were repointed to the new "§X.Y" section numbers.

- [WORK] Drafted all five explainer documents per `Explainer_Document_Plan.docx` and placed them
  in `40_DOCS/`: `Doc_1_Circularity_and_Game_Value.docx`, `Doc_2_Player_Pillar_I.docx`,
  `Doc_3_Player_Pillar_II.docx`, `Doc_4_Drafting_Prospects_Unbuilt.docx`,
  `Doc_5_Cross_Cutting.docx`. Written and voice-checked with the `thomas-voice-humanizer` skill
  (Thomas's verified sentence-level patterns plus the general AI-tell checklist, applied while
  drafting rather than as a cleanup pass afterward — zero em dashes, "each" over "every" where a
  set's members are being treated individually, intensifiers like "at all" and filler "itself"
  cut, contrastive "not X" tags kept only where the contrast does real identification work).
  Sourced from `Explainer_Document_Plan.docx`'s own "Must cover" / "Open / untested" bullets,
  cross-checked against `PROJECT_STATE.md`, `DECISIONS.md`, and `STANDING_FLAGS.md` for current
  figures, and drawing prose and structure directly from `Project_Status_Report_July_2026_UPDATED.docx`
  where it already covered the same ground in Thomas's own edited voice (data foundations,
  Steps 1-3 of the player pillar, Step 5's discount-rate framing, the circularity section, the
  ascending-young-players finding). Two places where the Status Report's numbers were stale
  against the current locked state were corrected rather than carried forward: contract counts
  (2,909 → 2,981, including the goalie count 318 → 390) and the RFA selection-bias figures (the
  Status Report's pre-fix "614 walks of 1,231 decisions" is kept as the history of why
  calibration was needed; the corrected "1,039 of 2,270 decisions dropped, true rate 27.0%"
  from the Stage 4 review resolution is what's presented as current). Each document validated
  against the OOXML schema (all passed); soffice still cannot render a PDF preview in this
  environment, so a visual check by Thomas is the outstanding step before these go to Karl.

- [CORRECTION] Docs 1, 2, and 5 named Karl and referenced the August 28 meeting directly
  ("Karl raised this twice in the same conversation," "Raised in the August 28 meeting") —
  wrong for documents that are themselves being sent to Karl, and an easy tell to write for a
  session working from meeting notes and session logs as source material without stepping back
  to ask who the deliverable's reader actually is. Rewrote the three affected passages (Doc 1's
  opening paragraph, one open-item note in Doc 2, Doc 5's opening line) to state the same
  substance without naming him or the meeting as the reason the document exists; swept all five
  documents afterward for the same pattern (`karl`, `meeting`, `thomas`, `supervisor`, `raised`,
  `feedback`) and found nothing further. Regenerated and re-validated Docs 1, 2, 3, and 5 (Doc 3
  was clean already; rebuilt anyway since the build script runs all of them together). Worth
  keeping as a standing check on the remaining pillar documents Thomas has not yet requested:
  before treating a session log or meeting note as source material for a reader-facing
  document, check whether anything meant for internal tracking is being carried into the
  deliverable itself.

- [CORRECTION] Thomas line-edited `Doc_1_Circularity_and_Game_Value.docx` directly (deletions, no
  track changes) and left 11 comments, flagging a broader set of AI-writing patterns than the
  earlier Karl/meeting issue: preview sentences, redundant closing-summary sentences, internal
  severity/resolution-date tracking language, meta-citation instructions, abstract personifying
  "the honest claim is X, not Y" rhetoric, and unexplained jargon (RAPM, strength/score state,
  convergent validity, "trailing_war(t)" code notation). Rewrote Doc 1 applying all of these:
  cut the flagged sentence types, unpacked jargon into plain sentences, retitled two headings,
  merged the redundant "three jobs" preview section into "Mid-season allocation." Word count
  1,391 → 1,219. Flagged two open questions rather than deciding them unilaterally: whether to
  keep the "Open and untested" section (the plan's Coverage Rule requires it; Thomas deleted it
  here with no comment) and whether the cut pre-registration paragraph (identification-rigor
  content) should be restored in plainer language. Not yet applied to Docs 2-5.

- [WORK] Applied the same edit patterns to Docs 2-5, confirmed by Thomas ("Yes"). Cut preview
  sentences, redundant closing summaries, and meta-instructions about how a finding should be
  cited elsewhere; cut the one self-congratulatory sentence about the document's own honesty
  in Doc 3's discount-rate section; replaced roughly 25 `(Doc N, §X.Y)` bracket citations across
  all four documents with plain in-prose references. Kept the "Open and untested" section in
  every document per the resolution already applied to Doc 1; kept the cut identification-rigor
  content (Doc 1's pre-registration paragraph) cut, matching the demonstrated pattern rather
  than restoring it. All four regenerated and validated against the OOXML schema.

- [CORRECTION] Thomas line-edited Doc 1 a second time, explicitly scoped to Doc 1 only
  ("don't rebuild any other doc"). This round's eight comments were substantive comprehension
  gaps, not voice complaints: "trailing production" / "projected production" were used
  throughout without ever being defined (five separate comments circling this one gap); the
  Game Value explanation had lost "goals for and against" (dropped to just "goals scored") in
  an earlier edit pass and never explained why the 58.3% goal-differential check isn't circular
  given the metric's own accounting identity holds exactly by construction; and the document
  never explained why mid-season allocation uses Game Value rather than simply splitting by
  point totals. Fixed all three with real added explanation, not just trims. Also: the "Open
  and untested" section was deleted again with no comment — second time in a row, so treated
  as confirmed intent rather than re-asked. Removed the section, folded its live content into
  the flowing prose of "The circularity check." Not yet applied to Docs 2-5 or to the plan's
  own Coverage Rule, per Thomas's explicit scope for this round. Word count grew, 1,219 → 1,484,
  since this pass added explanation rather than removing filler.

- [CORRECTION] Thomas: the Game Value explanation still just said "we take data and make
  number" — not enough given what it's inputs/transformation/outputs. Read the actual game-level
  scripts (`xg_model.py`, `score_state.py`, `metric_assembly.py`) rather than relying on prior
  summaries, and found the prior draft had it wrong: Game Value splits credit for expected goals
  (a logistic-regression shot-quality score from distance/angle/type/manpower), not actual goals
  scored. Rewrote "What Game Value is" with the real pipeline (xG score -> score-state
  adjustment -> equal split among on-ice skaters -> penalty layer -> one number per skater per
  game in goals) and fixed the explanation of why the 58.3% season-level check isn't circular:
  the real reason is that the metric prices shot quality, not actual goals, so checking it
  against real goal differential is a genuine outside test, not (as previously written) that
  the score-state weights "might be estimated wrong." Also fixed the mid-season allocation
  section's description of what Game Value is built from to match. Lesson for future sessions:
  when a reader's comment suggests the mechanism is being described vaguely, check the actual
  source code before rewriting the prose again, rather than restating the same summary in
  different words.

## Handoff / next

- Decide whether to drop the "Open and untested" section format from the plan's Coverage Rule
  and from Docs 2-5 to match Doc 1, or whether Doc 1's case is different enough to leave as is.
- Docs 2-5 have not been checked against source code the way Doc 1 now has been. Worth the same
  treatment before treating any of them as final.
- Thomas's read-through of the full five-document set as it now stands. These are complete first drafts, not yet Thomas-reviewed;
  expect a correction pass, especially anywhere a figure needed reconciling across sources (see
  the note above on the two corrections already made during drafting).
- Then Phase 3b/4 model work per `WORK_QUEUE.md` (Elite Prospects pull, mid-season allocation,
  standalone retention pricing, traded-pick pricing).
- `git pull` at session start — the v3.2 restructure changes where everything lives.
