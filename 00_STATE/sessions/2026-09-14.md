# Session 2026-09-14 - Whole-chain sweep to reduce the level tilt

- [CONTEXT] Thomas gave the go-ahead to experiment on the whole pipeline to reduce error in pricing, production projection and NPV at every level of player quality, including ideas raised earlier and not followed up. Follows the 09-13b findings (level tilt; fixed 2009-17 pull-back overshoots stars; market line refit does nothing).
- [DONE] `20_CODE/pipeline_experiment.py` v1.0: 19 variants through the production NPV engine, patched in-process, hashes checked. Every calibration rolling (fitted on 2009..t0-1). Starting-point rules L, L6, P, LB, LB3, RG, C (curve pass-through), M (market-informed, diagnostic); top-50 comparables; contracted-population hazard H; valuation-season hazard S0; combinations. Scored on the 4,677 played contract seasons. ~11 min.
- [DONE] `20_CODE/market_line_experiment.py` v1.0: 13 price-line specifications, rolling out of sample on 1,769 signings 2020-25, production censored estimator generalised. 15 s.
- [DONE] `20_CODE/games_line_experiment.py` v1.0: a games-aware value line (wins + games share) through the full chain with a rolling games projection, scored on its own currency. 2.5 min.
- [FINDING] The starting point is the lever. On 2020-25 pages (the fair test; the 2018-19 pages share the earlier fixed calibration window) rolling L takes bias from +3.5% to -2.9% overall and from +24% to -4% at 3+, cuts WAR error 5.7% and dollar error 2.8%, and removes the tilt (+$0.46M/WAR to -$0.01M). The variants of the rule land within 1-2 points of L. The earlier star overshoot came from the 2018-19 pages, where production was unbiased for stars and the pull-back ran 23% low; on 2020-25 pages production ran 24% high.
- [FINDING] The exit hazard is estimated on the wrong population: 10.6%/yr on all GP>=10 seasons vs 5.3% among players holding a contract for t+1; the chain prices only the latter. H alone is slightly worse on error (it had been offsetting over-projection); L+H is the best-calibrated combination (bias -0.5% overall, within 5% at every level, expected exit 6.2% vs 7.0% realized) and moves net NPV -$645M against -$1,186M for L alone.
- [FINDING] M (cap-hit-informed anchor) beats every stats-only rule by ~2.5 points; circular, so a ceiling, not a candidate. Top 50: -0.7% overall, -3.4% for 3+. S0 over-predicts exits with the production hazard. C under-corrects the 1-2 tier.
- [FINDING] Market line: error is not tilted by level (the over-valuation of stars is all projection). Admissible gains: one-season flag -5.3%, free two-season weights -6.3%, +age -7.2%, +games share -16.8%. Games carry their own price (~$2.1M full vs half season at the same WAR) and take the price per win from $2.03M to ~$0.8M. Contract length -30.6% but inadmissible (prices the contract off its own term); a back-test mispricing candidate.
- [FINDING] Games-aware line in the chain: relative error 56.5% -> 49.8% of realized value overall, 47% -> 40% for 3+, on its own currency; runs ~6% low; changes the D6-D9 definition of value. Design decision.
- [DONE] Report `50_REBUILD/docs/Pipeline_Experiment.md`. State files, manifest.
- [NOT DONE] No production change. Not run: rolling refit of the aging curve; RFA control years and goalies unscored; the mid-contract (trade-date) population.

- [CORRECTION] Thomas: contract term belongs in the value line as a candidate; excluding it is an omitted variable, and the free-agent alternative can be framed as one deal for the remaining term as validly as one-year deals each season. Agreed; "cannot be used" withdrawn in the report. What term carries is a mix of the price of commitment and private information, so a line with term moves the term premium from mispricing-to-measure into fair value: a back-test framing decision, recorded, not a rule. Thomas: find the lowest-error combination on performance, contracts and NPV, breaking locked decisions to test; nothing ships by being tested.
- [DONE] Second pass. `market_line_search.py` v1.0 (stepwise over WAR blocks x {games, one-season flag, age, age², RFA, term, term², term×WAR, age×term}, selected on 2020-22 signings, scored on 2023-25). `pipeline_experiment.py` v1.1 (main(variants, prefix); LA and LB3A rules with age) + `pipeline_experiment2.py` (11 variants). `games_line_experiment.py` v1.1 (rolling production spec, +games, +games+term through the chain).
- [FINDING] Contracts (held-out 2023-25, 842 signings): production $1.235M; best term-free line (two seasons free + games + one-season flag + age²) $0.997M (-19%); best with term (+ term, RFA, term², age×term) $0.755M (-39%), unbiased within $0.15M at every level but 2-3. With term and games in, a win prices at ~$0.9M/yr, a season of term ~$0.45M/yr, a full season of games ~$2.7M.
- [FINDING] Performance (2020-25 pages): age in the pull-back (LA, LB3A) keeps the tilt at zero, lifts young players and lowers older ones, and makes the package near NPV-neutral (LB3A -$382M; LB3A+H +$153M; LB3A+H+top50 +$71M vs L -$1,186M). LB3A: WAR error -6.4%, bias +0.2% overall, weak spot the 0-1 tier (+4 to +7%). Stats-only ceiling remains ~2 points above (market-informed diagnostic).
- [FINDING] NPV with lines: the production spec refitted rolling behaves like the locked line (in-sample fit is not what holds it up). Term in the line adds $3.3-4.1B of NPV across 2,591 contracts (the term premium counted as value); L + games + term leaves stars at production NPVs (3+ -$0.05M) with the tilt gone; term-free, stars fall $9-10M each. The term line's lower dollar error is mostly repricing (a WAR miss costs less at $0.89M/win), not better prediction.
- [DONE] Report second-pass section in `50_REBUILD/docs/Pipeline_Experiment.md`; first-pass term sentences corrected.

## Open threads

- Decide whether to adopt L+H (+ top 50): changes the k=0 Layer 1/Layer 2 identity and the hazard population in `exit_hazard.py`; needs a deliberate revisit with full-chain movement recorded.
- Decide the value-line framing (D6-D9): term-free (one-year replacements) or remaining-term (term in the line, +$3.3B aggregate NPV, term premium becomes fair value). Then the games share. Possibly after the draft and prospect pillars are built.
- Decide the realized-value currency for the back-test (from 09-13b) before any star-vs-package result is read.
- If the term-free framing is kept: contract length as a priced term with no production behind it is a Phase 4 mispricing candidate.

# Session 2026-09-14 - Elite players and the aging curve's comparable weighting

- [CONTEXT] Thomas raised two linked questions about why elite players are structurally awkward for the model. (1) Is the position-and-age average doing more harm than good, i.e. does the curve let too little player-specific performance into the estimate? (2) Specifically, is the fixed pooled weight of ten deflating elite players, because elite players have fewer comparables and so a smaller similarity-weight sum? He quoted the passage in `40_DOCS/Supervisor_Drafts/Data, Production, and Aging.docx` that illustrates the split with comparable weights summing to twenty, then to five.
- [FINDING] Read from `aging_curve.py` rather than the explainers. Both shrinkage steps in the anchor were measured with a new read-only audit. **Question 2 is measurably not the problem. Question 1 is, and by more than the question assumed.**
- [FINDING] The pooled weight of ten is worth about **4-5% of the estimate at every level band**, elite included (median similarity-weight sum 190-240 against a fixed ten). The explainer's illustrative sums of twenty and five imply a pooled share of a third to two-thirds, roughly an order of magnitude above the real regime, and they point a reader at exactly the wrong lever. Elite players do **not** carry deflated weight sums: their median sum is 194 against 236 for the mid band, and their pooled share, 0.049, is indistinguishable from the negative band's 0.050.
- [FINDING] The real lever is the **bandwidth**. `h` is the median within-position pairwise distance of the whole cloud (2.526, matching the value in the production docstring), so the Gaussian kernel is nearly flat and barely selects. Measured retention slope — the share of a player's deviation from his position-and-age cell that survives into the comparable estimate — is **0.193 for forwards and 0.167 for defencemen**, falling only to 0.180 / 0.151 after the pooled weight is applied. The anchor therefore keeps `LAMBDA + (1-LAMBDA) x slope` = **0.631 of a forward's deviation, and 0.637 is all that setting SHRINK_K to zero could buy.** Six tenths of one percentage point.
- [FINDING] The comparable set is not comparable at the top. For a median elite target at 3.73 WAR/82 the comparable estimate lands at **1.58**; only **7% of the comparable weight** comes from players within half a win per 82 of him (37% for the `lo` band); the Kish effective number of comparables is **369 against a pool of 460**, so the kernel is close to an unweighted cohort average. Nineteen genuinely similar players sit in that pool and carry 7% of the weight.
- [FINDING] **The deflation does not cancel in the ratio, it is amplified by it.** `skater_forward_projection.ratio_path()` takes `base = levels[0]`, the mean-reverted level, and prices off `l / base`, then applies that ratio to the player's unshrunk 60/40 raw-WAR anchor. The forward steps in the numerator are age-conditional deltas in absolute WAR units and do not scale with the level. A shrunken denominator with a same-sized numerator increment therefore **steepens the projected percentage decline** for exactly the players whose anchors are shrunk most, compounding across the back half of the long contracts elite players sign. The module docstring's "we take only its SHAPE ... the units stay regression-native" holds only if the deltas scale with the level, and they do not.
- [FINDING] Where the pooled weight of ten **does** bite is thin **age** cells, not high **level** cells — already measured in `40_DOCS/Aging_Curve_Coverage_Audit.md` Finding 4 (league-curve share 78% at 19, 31% at 20, 49% at 37). Elite players on eight-year deals age into that thin end, so the pooled term and the ratio amplification meet in the back half of a max-term contract. That interaction is untested.
- [FINDING] `SHRINK_K` is also a divide-by-zero guard, not only a weighting choice. `_shrunk` masks candidate weights to those with an observed value at the age being estimated, so the sum is exactly zero whenever no comparable has one — routine for the delta column at the oldest ages. At `SHRINK_K = 0` that is 0/0 and the projection returns NaN. Found by running it; the test arm uses an epsilon instead, so it tests removing the pooling rather than removing the guard.
- [DONE] Two new scripts, neither touching a production constant or a file the chain reads.
  - `20_CODE/aging_comp_weight_audit.py` v1.0 — read-only. Reports the fitted bandwidth, the retention slope by position before and after pooling, and comparable-set quality by level band (share of weight from similar players, Kish effective comparables, pooled share). Writes `30_OUTPUT/aging_comp_weight_audit.csv` with per-age detail, since the band table pools the age effect away.
  - `20_CODE/aging_elite_shrinkage_test.py` v1.0 — nine-arm held-out comparison on the bandwidth test's design: production, three `SHRINK_K` settings, three bandwidth scalings, the joint cell that `40_DOCS/Aging_Yardstick_Comparison.md` records as untested, and a reliability-weighted lambda (`n/(n+n0)`, with `n0` solved on **training** careers so the arm reproduces lambda = 0.55 at a median-length record and only redistributes). Reports **signed bias within fixed anchor-level bands** on both the curve and the season-total endpoint, because pooled MAE is dominated by the middle and hides a tail-confined bias — neither the production `validate()` table nor the bandwidth test reports it. Reuses `truncated_player` and `ratio_prediction` from `aging_bandwidth_test.py` by import rather than by copy, so the two tests cannot drift on holdout semantics.
- [DONE] Verification. Both scripts executed end to end in this session. The production files are hashed before and after and the run asserts they did not change. Two real defects were found by running rather than by reading: the NaN at `SHRINK_K = 0` above, and a hash-keying bug that assumed `OUTPUT_DIR` sits inside the repository (`aging_bandwidth_test.py` carries the same latent assumption and will break the same way if `OUTPUT_DIR` is ever set outside the repo).
- [NOT DONE] **Neither script has been run on real data, and no number here is a result about aging.** This session ran in a remote container with no PuckPedia birthdate export, so `OUTPUT_DIR/WAR_with_age.csv` could not be built. The retention slope, bandwidth, pooled share and comparable-set quality above are transferable because `h` is a pooled within-position pairwise median that does not depend on the age partition, and the level bands come from real WAR — but the **age cells were synthetic**, so pool sizes are flatter than real ones and the per-age figures in the CSV mean nothing until the real run. The nine-arm test produces nothing interpretable at all without real ages. Both need running on the desktop machine.
- [NOT DONE] No production constant changed. `LAMBDA = 0.55`, `SHRINK_K = 10` and the bandwidth rule are untouched, and D1-D27 are not reopened.

## Open threads

- Run both scripts on the desktop machine against the real `WAR_with_age.csv`. The audit takes seconds; the nine-arm test is the bandwidth test's cost with more projection arms.
- The nine-arm test sweeps many arms against one evaluation set. A winning arm is a lead to re-test on the historical-window mode, not a result — and any change to the bandwidth rule or lambda is a deliberate revisit of a locked decision, with a lambda re-test and full-chain movement, on the terms the coverage audit already set out.
- The fat-tailed-prior argument is the version of Thomas's question that survives contact with the code: a constant retention is optimal under a Gaussian prior, and NHL talent is plainly not Gaussian at the top. Whether to shrink on a transformed scale, use a fat-tailed prior, or cap the pull with a limited-translation rule is undecided, and the reliability-lambda arm is the level-blind version that is hardest to mistake for tail-fitting.
- `40_DOCS/Supervisor_Drafts/Data, Production, and Aging.docx` needs its pooled-weight examples corrected before it goes further; they are the reason the question landed on the wrong lever. The document is also silent on the retention slope, which is the number a supervisor would actually want.

# Session 2026-09-14 - Cap inflation in the NPV, and whether club discount rates can be recovered from trades

- [CONTEXT] Question-and-answer session, run in a cloud container. No `.env` and an empty `30_OUTPUT/`, so nothing was run against the spine; every figure below is either read out of `20_CODE/` or computed analytically inline from constants read there. No script was added, changed, or executed. No model change.

- [QUESTION] Thomas asked whether the model understands that a contract holds a flat AAV while value is compared on a cap-share basis, so that a player worth $8M today is worth $8.4M next year under 5% cap growth, and whether a contract signed a year earlier therefore carries surplus against the same contract signed a year later.
- [FINDING] Yes, and the mechanism is `cap_path()` in `skater_forward_projection.py` (lines 381-386): `CAP_CEILING[t0] * (1 + CAP_GROWTH) ** k`, with `val_raw = (ALPHA + _slope * pw) * ceil` at line 727. Value is nominal dollars on a growing ceiling, so a constant cap share converts to a rising dollar figure. Cost is `cs_cap_hit` filled from `pp_cap_hit` (line 523), read per contract-season rather than assumed flat, so ELC bonus years and 35+ cases come through as they are. `contract_npv.py` then discounts at the same constant (`G = CAP_GROWTH`, line 197; `disc = (1 + G) ** (-k)`, line 426). Illustration at g=3%, five years, identical players and terms: a cap hit set a year earlier prices at +$3.463M NPV against +$2.331M for the same cap share priced a year later, a gap of $1.132M, which is five times the $0.24M nominal price difference and does not decay in k.

- [QUESTION] Thomas then argued the discount rate erodes the benefit of signing long term, since part of that benefit is the declining cap share a flat cap hit occupies, and separately that a uniform rate assumes each club values the future identically.
- [FINDING] The first half is the reverse of what the code does, and the correction matters because it is load-bearing. Growth and discount are one constant, so they cancel on the value term (D24) and the surviving `cost / (1+g)^k` is the declining cap share itself. There is no pure time discount in cap-share space: year-8 surplus counts as much as year-1, and the only brakes on term are the exit hazard (D18: 1.1%/yr star, 0.9% regular, against 10.3%/19.4% fringe and below-replacement) and the aging curve. Term is rewarded monotonically, at +$0.23M / +$1.37M / +$6.16M for two, four, and eight years at g=3% on a flat cap hit equal to t0 value.
- [FINDING] The premise that long term lowers AAV is also backwards in this sample. The length specification in `term_premium_test.py` (lines 8-10) carries `c_len = 0.00859642`, or +$0.82M of annual cap hit per extra year of term, controlling for trailing production. Long deals show negative NPV because clubs pay a term premium that Stage 2 found buys no measurable production (main spec f = -0.0135, 95% CI [-0.0360, +0.0089], value-side share 0.000), not because the discounting penalises length.
- [FINDING] **NEW, and the reason for the new standing flag.** The version of the objection that does hold runs through g rather than through discounting. Because the cost-erosion channel runs at exactly g, raising g raises the reward for term: the same eight-year deal moves +$6.16M at 3%, +$9.71M at 5%, and +$13.77M at 7.6%, against realized ceiling growth of 7.6%/yr from 2023-24 to 2026-27. The existing published-ceiling flag records this constant as a level error; it is also a term-gradient error, and that framing makes part of the long-contract negative-NPV finding a candidate g artifact. Stage 2 tested length on the value side and did not test the growth assumption, so this is an untested competing explanation, not a contradiction of the null.
- [FINDING] The second half of the objection is correct and is D15 working as designed: the rate is normative so that club impatience is a back-test finding rather than an input. Two standing flags already cover the substance (club-specific versus league-average value, contender versus rebuilder marginal-win value). The sharper point is that heterogeneous time preference is why trades clear at all, so a rebuilder trading a win-now player to a contender at mutual advantage is efficient gains from trade, and the back-test scores it as mispricing against one league-average yardstick regardless.

- [QUESTION] Thomas asked whether differences in clubs' assumed r could be found by era or build timeline, assuming the rest of the NPV model is sound.
- [FINDING] The sequencing inverts. Identification needs time-profile spread between the two sides of a trade, which lives in the 654 of 946 groups involving a pick (69.1%); the 205 clean player-only trades are the sample with the least of it. Trade-sides per club are roughly 13 on the clean 205 and 59 on all 946, so club-specific rates are not estimable here even though a two-sided fixed-effects decomposition over the trade network is the correct design in principle. Era differences and a club-state covariate are estimable; club effects are not.
- [FINDING] The shelved pure-pick finding does not kill this. It killed a level, because corner solutions under the minimum-denomination floor make equality-based estimation misspecified (per-trade implied values 0.015 to 0.689 around a fitted 0.486/0.510). A corner still yields an inequality, and if the censoring is common across groups it differences out. The forward path already recorded with that item, bounds rather than a point, is the right one, and sweetener size scaling with gap at Spearman 0.735 is the evidence the bounds will be narrow enough to compare.
- [DONE] New sequence document `00_STATE/04_Discount_Heterogeneity_Sequence.md`, written to the house style of 01-03. Eight steps: settle g first; decide levels versus differences; fix the bargaining restriction in writing before estimating (one price, two rates, so either the directional or the symmetric-split restriction, chosen in advance); build the club-state covariate ex ante from the `games` table in `nhl_gamelog_scraper.py` (`game_date`, `away_team`, `home_team`, 11,870 games 2017-18 to 2025-26), using points percentage and games remaining at the trade date and never final standing; the repeat-sales leg split by era (163 picks at two or more horizons, roughly 54 per era, with the back-filled `overall_position` trap called out because a repeat-sales ratio would hide it); the full-population leg with rho as a function of state rather than 32 club parameters; a long-horizon projection check against Game Value at k=4..8 before any figure is quoted; and power plus a pre-registered reading rule.
- [FINDING] The step 7 risk is live rather than hypothetical. The regressor of interest is horizon and `_proj_sd_war` holds the k=3 spread flat beyond k=3, covering about 13% of projected seasons, all on long contracts. The 2026-09-13b result (stars over-projected +$0.98M per season at 1+ WAR, +$1.49M sustained, mostly the unshrunk starting level) is a horizon-correlated bias in the value side, already measured, in the population long contracts are drawn from. Clubs discounting the future and the model being wrong about the future leave the same residual.
- [DONE] State: new STANDING_FLAGS entry at the top of the flags list (g and rho not separately identified); WORK_QUEUE Phase 2 second arm on the power-analysis re-run and new Phase 4 item 4e; DECISIONS change-log entry; MANIFEST row for the new document; this session file.

- [NOT DONE] The g sensitivity itself. Re-running back-test surplus differentials at g in {3%, 5%, 7.6%} split by contract length would separate the term premium from the growth assumption in the long-contract finding. It needs the spine and a machine with the paths, so it was not attempted here.
- [NOT DONE] No production change. D11, D17, and D15 are untouched and stay locked. Nothing in the pipeline was run, so no figure in this session is a fresh run output.

- [CONTEXT] Thomas then asked for the two manifest issues to be fixed. Audited MANIFEST.csv against `git ls-files` rather than fixing the two by hand, which found more than the two.
- [FINDING] Scope is defined in PROJECT_STATE's MANIFEST.csv section: every git-tracked file under `20_CODE`, `00_STATE`, `10_SOURCE`, and `40_DOCS`, plus the `30_OUTPUT` working set and the game-log DB. Repo-root files and `90_ARCHIVE` are therefore out of scope and are not missing rows, which removes 11 of the 26 apparent gaps.
- [FINDING] In scope: **9 stale rows** for files no longer in git, **15 missing rows**, and **5 rows whose hash had drifted**. The stale rows are the retired `00_Immediate_Work.md`, four top-level `Doc_N` explainers, `Docs_3_4_5_Review_Notes.md`, and the four `Supervisor_Drafts/Doc_N` copies that the four human-titled documents replaced. The missing rows are 9 session files (2026-09-09b through 2026-09-12), `Design Questions.txt`, the four human-titled `Supervisor_Drafts` documents, and `Doc_3_Player_Pillar_II_2.docx`.
- [FINDING] **A real defect, not a bookkeeping one: `40_DOCS/Doc_3_Player_Pillar_II_2` had lost its `.docx` extension.** `sessions/2026-09-11c.md` records the file being edited as `Doc_3_Player_Pillar_II_2.docx`, `file` reports Microsoft Word 2007+, and it opens as a valid zip. It entered the repository without the extension in the 2026-09-13 19:54 sync commit. Restored with `git mv`, which is a repair rather than a filing decision; the no-rename rule covers `20_CODE` and `30_OUTPUT`, where names are load-bearing in code.
- [FINDING] **Caught mid-fix: refreshing rows on a size mismatch would have corrupted the manifest.** The first pass refreshed 56 rows because `size_bytes` differs from disk on most text files. The hashes match; the recorded sizes were taken on a CRLF machine and this container is LF, so `contract_npv.py` reads 42,810 bytes in the manifest against 42,026 here on an identical blob. Writing the LF sizes would have made each row mismatch on Thomas's own machine. Redone on hash only, with recorded sizes left alone, which is consistent with the protocol's own rule that size is not an integrity check.
- [FINDING] Corrected my own earlier edit. The six rows this session created were set `verified_locally=no` on the reasoning that nothing had been checked on Thomas's machine, but the protocol defines the field for `channel = git` rows as yes whenever the working tree is clean, since git is the audit for those rows. Set to yes.
- [DONE] MANIFEST.csv reconciled: 131 rows to 137 (9 dropped, 15 added, 5 hashes refreshed, 6 `verified_locally` corrected, the self-row's note changed to say the hash cannot be self-referential). Re-audited after writing: no stale rows, no in-scope gaps, no hash mismatches, no duplicate paths, and every row carries 11 fields.

- [CONTEXT] Thomas asked for both remaining manifest findings to be fixed.
- [DONE] Untracked `90_ARCHIVE/2026-09-12/00_Immediate_Work.md` with `git rm --cached`. The file stays on disk and `git check-ignore` now confirms `.gitignore:22` covers it, so the repository matches the stated intent that `90_ARCHIVE/` is not committed. Recoverable from history if it is ever wanted back.
- [FINDING] The size column was worse than the previous entry recorded, and not my doing. Measured across the git rows: **52 carried CRLF sizes, 34 carried LF sizes, and 3 matched neither.** The LF population was not the 11 rows written this session; sessions 2026-09-09b through 2026-09-13b and five `20_CODE` scripts (`aging_bandwidth_test.py`, `aging_comp_limit_test.py`, `anchor_shrink_test.py`, `npv_realized_by_tier.py`, `player_dashboard.py`) were already LF, so earlier cloud sessions had been writing LF sizes for weeks. The column has been disagreeing with itself since at least 2026-09-09.
- [FINDING] Two of the three mismatches were mine. Refreshing hashes without sizes in the earlier pass left `AGENTS.md` and `sessions/2026-09-09.md` with a current hash beside a size from a much older version (6,953 bytes recorded against 22,662 actual). Hash-only was the right call for rows whose hash matched; it was the wrong call for rows whose hash had drifted.
- [DONE] Fixed the definition rather than the rows. `size_bytes` on `channel = git` rows is now the **git blob size**, reproducible anywhere with `git cat-file -s <blob>` and equal to the LF byte count, which was verified against `stat` on three files first. This ties the column to the same object the hash column identifies and takes the writing machine's platform out of it. 57 rows changed, 52 down from CRLF and 5 up from stale, and 47 were already correct. The 32 `dropbox` and `local` rows were left alone, since those files do not travel through git and their sizes come from real local runs.
- [DONE] Convention written into PROJECT_STATE's MANIFEST.csv section, with the 42,810-against-42,026 example, so a future session does not reintroduce a `stat`-based size. Size remains a staleness signal rather than an integrity check.

## Open threads

- Run the g sweep at {3%, 5%, 7.6%} on `contract_npv_spine.csv`, split by contract length, and report how much of the long-contract negative NPV is the $0.82M/yr term premium and how much is the 3% growth path. This gates item 4e and bears on a headline finding.
- Decide whether the g question is a deliberate D11/D17 revisit or stays a reported sensitivity. The revisit moves each NPV artifact, so the sensitivity is the cheaper route and may be sufficient.
- The league-minimum schedule uses published CBA figures while the ceiling uses flat 3%, so the two sides of the D10 floor disagree for t0 >= 2025. Already flagged; small, and touches floor-bound fringe rows only.

