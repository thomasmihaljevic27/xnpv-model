# Session 2026-09-25b - Which GitHub branches belong in main, and a script to merge them

- [REQUEST] Go through the GitHub branches, decide which should be merged, and write a script to do it.
- [FINDING] `main` is Thomas's review line (state files and review documents). The rebuild's code
  lives only on `claude/amazing-johnson-cllbgl`, 142 commits ahead. Of eleven other branches: three
  are already in main (`50_rebuild`, `claude/affectionate-galileo-mh9f7w`,
  `claude/relaxed-clarke-onaulu`); `claude/loving-maxwell-rd3w5v` is contained in the rebuild branch;
  four carry unmerged work (`player-valuation-walkthrough` 2026-09-25, `awesome-curie` plain-language
  overview 2026-09-24, `elite-aging-curve-weighting` and `cap-adjusted-contract-surplus` 2026-09-14).
- [SKIP] `claude/wizardly-goodall-25okn3` is the pre-migration repository with no common ancestor;
  merging it would revert about 4,000 lines. Its only unique content, three CLAUDE.md rules from
  the 2026-09-11 Doc 2 review, was ported into CLAUDE.md here. `claude/nifty-euler-u5c5vm` enables
  an unpinned third-party plugin that was never loaded or verified; left out, opt-in flag provided.
- [DONE] Merged `origin/main` (decision guide, thesis-scope council) into the rebuild branch; three
  state-file conflicts, each two same-day appends, kept both sides.
- [DONE] `integrate_branches.py` v1.0 at the repo root: checks the plan against the remote (pinned
  tips), merges in order onto a local `integrate/<date>` branch, auto-resolves only pure-addition
  conflicts in the state files (diff3 ancestor check), keeps main's MANIFEST.csv, verifies (no
  markers, Python compiles, each branch contained, fast-forward possible), pushes only with `--push`.
- [OPEN] MANIFEST.csv needs a re-audit after the merge (the 2026-09-14 reconciliation is not carried).
- [FIX] First run on THOMASLAPTOP stopped: v1.0 refused any untracked file, and the untracked files
  were the script's own copy (which the rebuild branch's merge would also have collided with) and
  `.env.bak_20260925`. A stale `.git/index.lock` separately stopped `sync.ps1`. `integrate_branches.py`
  v1.1 builds in a temporary git worktree, so the folder it is run from is never touched; keeps each
  file's line endings (CRLF checkout); reads git output as UTF-8. Tested on a CRLF clone on the
  walkthrough branch with both untracked files present: same result tree as the LF run
  (`9a41fec`), no CR bytes committed, folder and branch unchanged.
- [FIX] `.gitignore`: `.env.*` ignored except `.env.example`. The repo is public and `sync.ps1` runs
  `git add -A`, so `.env.bak_20260925` (machine paths) would have been committed.
