# 2026-09-16: taking the coverage gap apart, and finding the participation model in it

The uncertainty repairs were verified clean the night before: all three findings closed, 22
checks passing with no skips, and the reviewer's own adversarial test confirming the new
distribution-mean guard fires when the fix is disabled. What was left was not a bug. It was the
open model question both reviews had now asked in the same words:

> The current evidence does not cleanly separate how much comes from forecasts being too low,
> uncertainty ranges being too narrow, or participation estimates.

That is a well-posed question, and it is also the first item on the queue, so it was the work.

## How it was asked

Three levers, each set to the value the outcomes say it should have had for that subgroup at that
horizon, coverage recomputed with each one alone and with all three: shift the centre until the
group's standardised misses sit where the band's shape says, stretch the band until its realized
span matches, replace the predicted probability of playing with the share who did.

Every one of them uses the answer. That is the point and it is stated three times in the file: it
measures the **ceiling** a perfect repair could reach with hindsight on this sample, which is an
upper bound and not a forecast of a real repair. A lever that cannot close the gap with hindsight
certainly cannot close it without.

Because an oracle says how much a lever could buy and not what is wrong with it, two things were
also measured with no hindsight at all: the participation probability against the realized play
rate, and the season-total bias split into rate, games and participation.

## What came back

**No single lever closes the star gap.** Five seasons out the best of the three closes 36% of the
distance to the stated 80%, and all three together overshoot it. Which one dominates changes with
distance: the centre near the valuation season, the spread and participation further out. The
star gap is three defects, not one, and I had been writing about it as one.

**For players 22 and under the centre is the wrong lever.** Correcting it makes their coverage
*worse* at five of six horizons. The spread carries 47 to 110% and participation 31 to 62%. The
oracle shift for them is near zero at the short horizons, so the forecast is not systematically
low for them where the gap opens. The queue's stated fix for this population — a prior from the
prospect pillar — addresses a component that is not the problem.

**Participation is squeezed toward the middle, and it is the finding of the day.** Predicted
against actual, one direction the whole length of both tables: 22-and-unders 0.677 against 0.804
five seasons out and under-predicted at *every* horizon; 3+ 0.781 against 0.883; 2-to-3 0.682
against 0.799; and the other way, 34-and-overs 0.491 against 0.373 at the valuation season. The
groups that survive are under-predicted and the groups that do not are over-predicted. That is
under-dispersion: the model shrinks every group toward the league.

**The aggregate could not see it.** Predicted 0.752 against 0.710 actual at the valuation season,
0.352 against 0.383 five out — the two halves of the error cancel — and the Brier score sits at
0.126 to 0.138 at every horizon with no trend. A single calibration number over a population
containing both halves of a compensating error is blind to it by construction. Worth remembering
the next time a flat metric is read as reassurance.

**The point bias agrees from the other side.** The stars' bias is almost entirely rate: −0.921 of
a −0.866 total five seasons out, with participation contributing +0.002 at three out. The
22-and-unders at three seasons out are the opposite: rate off by −0.072, total off by −0.340,
participation off by −0.114. A group whose total is badly biased while its rate is not has a
participation problem wearing a forecasting problem's clothes.

## What it changes

Participation moves to the front of the queue. It had been untouched Phase 2 work with no known
defect beyond the population question; it now has a measured, one-directional defect that reaches
both of the populations the model is worst at, and it is the only lever that helps every
under-covered group.

The star residual keeps its place and loses some scope. It is real, it is confirmed twice here,
it is a **rate** defect worth about nine tenths of a win a season five years out for the players
who carry the money — and it closes at most a quarter of the star coverage gap at that horizon.
It should not be sold as the whole of the star problem, which is how this branch was describing
it yesterday.

The band's own tier-or-age spread term comes off the deferred list. It had been parked until the
centre was fixed. For the young it is the largest single lever at every horizon and a centre
repair would not help them, so the deferral was waiting for nothing.

## Limits, said plainly

Every lever is an oracle. The star subgroup is 203 scored rows a horizon and 171 five out, with
far fewer careers behind them, so those figures are the least stable in the report. Levers
substitute for one another and a nonlinear interval does not decompose exactly into three causes,
which is why the overshoot column is printed. Coverage is not accuracy. Nothing in the document
is fitted for use and nothing is in the chain.

## Not touched

No production code. No locked decision. No reserved page. The confirmatory seal is intact.

---

## The fact check, and nine things I withdrew

Astra checked `c4c41f4` and reran the diagnostic. The tables reproduce. Most of what I concluded
from them does not, and two of the errors were contradicted by this file's own printed output
while I was writing the sentence.

### The levers were never ceilings

I called each adjustment a ceiling — the most a perfect repair could buy — and then reasoned that
since no single one closed the star gap, the gap must be three separate defects.

The adjustments are arbitrary. Align a median, match a middle-90 span, substitute a group mean.
None of them maximises coverage, and knowing the outcomes does not turn an arbitrary adjustment
into the best available one. Astra killed it in one line: **doubling the spread alone takes star
coverage five seasons out from 60.8% to 88.3%**, past everything I had reported as a bound. That
is not a proposal — coverage alone always rewards a wider band — but it is a counterexample, and
the bound and everything resting on it go with it. "Three defects, not one" and "the centre closes
at most a quarter" are withdrawn.

### The bias table was four units pretending to be one

I printed a season total in wins, a rate in wins per 82, a games error as a fraction of the
schedule and a participation error as a probability, and read them as contributions to the same
number. A probability error of −0.114 is not −0.114 wins.

The exact accounting, substituting each realized value in turn so the parts add up:

| group, three seasons out | total | participation | rate | games |
|---|---:|---:|---:|---:|
| 3+ | −0.647 | −0.004 | **−0.507** | −0.135 |
| 22 and under | −0.340 | −0.061 | −0.072 | **−0.207** |

My implementation reproduces the reviewer's figures to three decimals, which is the check that
mattered. **The young players' bias is mostly games played, not participation.** I had it wrong,
and the units error is exactly how.

The stars' rate finding survives unchanged.

**One thing came out of the correction that is worth keeping**: games played is the largest single
component of the young players' bias and the whole of the stars' bias at the valuation season, and
nothing in this rebuild has ever looked at it. It has been the quiet half of the rate-and-games
pair throughout.

### A comment that asserted a number I never measured

The diagnostic used the last page's residual shape for every page, under a comment saying the
pages differed by less than a hundredth at every quantile. I did not compute that. The largest
difference is 0.030, and the shortcut moved the star five-seasons-out baseline from its true 60.8%
to 60.2%. Each page now uses its own shape.

This is the project's own most-repeated rule — do not describe a mechanism you have not read, do
not state a figure you have not run — broken in the one place designed to make a shortcut look
checked.

### And four smaller ones

The participation error is not uniformly one-directional; stars are slightly over-predicted at the
short horizons and below-zero players under-predicted at the long ones. Participation does not
help every under-covered group; my own gap-closed table printed −16% and −4% for the 1-to-2 tier
while I wrote the sentence saying it did. A flat Brier score does not show subgroup errors
cancelling — a squared-error score does not cancel by sign; what cancels is mean predicted against
mean actual. And the star rows are 81 to 88 distinct careers, not the 171 to 203 rows I quoted.

All nine are listed in the report's section 5 rather than edited away, because the earlier version
was published and read.

## What survives, and it is the paragraph for the write-up

> The model has systematic forecast and interval errors for some age and ability groups,
> concentrated at longer horizons. Nominal 80% ranges hold 82-84% of outcomes overall but 61% for
> the highest trailing-ability group five seasons out and 66% for players aged 22 and under.
> Participation probabilities are miscalibrated for those same groups. The respective roles of
> participation, games played, the rate forecast and selection are unresolved.

Selection is a plausible contributor and is not identified. The harness already keeps departed
players as zero seasons rather than dropping them, and the aging curve already attempts a
correction, so this is not simply the error from testing on survivors. Identifying the mechanism
needs assumptions about who becomes observable, and a calibration table cannot supply them.

## The loop is closed

Thomas's call and the reviewer's recommendation agree, and they are right. Repeated adjustments to
one development sample that has been inspected many times cannot separate these causes, and each
pass spends credibility on a question the exercise is not built to answer.

The question that decides whether any of this matters is whether the thesis's own conclusions move:
do the valuations and the trade-mispricing categories survive reasonable forecast alternatives? If
they do, an imperfect forecast still supports the argument and the paragraph above is a
limitation. If they do not, that is a finding about the thesis rather than about calibration. That
is the next work, and the rest of the milestone follows it.

---

## Later: the question that decides whether any of it mattered

With the diagnostic loop closed, the work was the test both Thomas and the reviewer named: do the
valuations and the mispricing categories move when the forecast is varied — and how does the live
chain compare?

**First the environment.** The two production-adapter checks had been skipping for want of the
PuckPedia `.xlsx`, because production reads it with `read_excel` and only the CSV was here. The
CSV was read through the tree's own encoding-detecting reader and written to `.xlsx`; all 40
columns come back identical on both nulls and values. `20_CODE/age_join.py` then builds
`WAR_with_age.csv`, which is what the adapter needs. The suite runs **22 passed, 0 skipped, 0
failed** — the same as the reviewer's machine, so the live chain can now be priced here.

**The design.** Same 1,217 contracts, same cost side, same signing-dated rolling protocol, same
discounting and cap path, same seal. Five forecasts vary: the live chain, a trailing blend, the
calibrated total with and without participation, and the adopted candidate. Each gets its own
price line, because the currency is fitted on the forecast and a different forecast is a different
market. That makes the level incomparable across columns by construction and the gradient
comparable, which is the half the thesis rests on.

## What came back

**Three of five categories hold under every forecast, and the live chain agrees about all three.**
Below replacement is always negative, 0-to-0.5 always positive, and 1-to-2 wins always positive
and the largest solid category at +0.75 to +1.76 $M. Contract by contract the rebuilt forecasts
correlate 0.974 to 0.988 with the live chain, agree on sign for 88 to 97%, and differ by $0.24 to
$0.47M on deals worth millions. The two models are not describing different worlds.

**The top tier flips sign.** Live chain −0.19, trailing blend +1.05, the three rebuilt forecasts
−1.7 to −1.8. On **eighteen contracts**, from a sample that keeps only about half the six- and
eight-year deals.

That is the second independent reversal of the same headline. The currency-shape work already
found surplus at the top flipping between a straight and a log price line. This is a different
axis entirely and it flips it again. **The elite tier is not one design decision away from being
settled** — two defensible choices each reverse it, and the queue's standing instruction that no
back-test result about stars should be cited is now doubly evidenced.

**Removing the participation model changes almost nothing.** Top tier −1.68 against −1.74 with it,
correlation with the live chain 0.977 against 0.974. Two days of work went into characterising
that model's subgroup miscalibration, and on this evidence it does not move the valuation
ordering at all. Worth knowing before spending a phase on it; it is a reason to fix participation
for the forecast's own sake rather than for the thesis's.

**The term framing dwarfs every forecast choice.** Top-tier surplus swings $9M to $14M a contract
between term-in and term-free, against a $2.9M spread across all five forecasts. A framing
judgement is worth three to five times any modelling difference measured here. That ratio belongs
in the write-up, because it says plainly where the uncertainty in this model actually lives.

## Two things found in passing

Term coverage is 64% of eligible development contracts, and the loss is selective: about two
thirds of one-year deals survive against half the six- and eight-year ones. Long contracts are
where the top tier lives, so the thinnest part of the sample is thinned further by the mechanism
meant to protect it. Extending the forecast's reach is the cheapest real gain on the board.

`A2AgingParticipationImputed` raises an `AttributeError` on `fitted_horizons_` inside its own fit,
before any contract is priced, so a candidate the variant register carries as live cannot be run
through the contract path at all. Recorded, not repaired here.

## Where this leaves the thesis

The back-test can proceed on the three categories that survive. The elite category cannot be
cited, and the reason is no longer a calibration problem — it is eighteen contracts and two
framing choices. That is a better-posed problem than the one this week started with, and it is not
solved by a better forecast.

---

## The valuation review: my headline was an artifact of my own grouping

Astra reproduced the run and found three things. All three are right and every corrected figure I
computed matches theirs.

### The tiers were different populations

I cut each column's tiers from that column's own forecast, and I documented the choice — "a claim
about stars means the players that model calls stars." Then I read the spread across those columns
as the same elite contracts switching between bargain and overpay. They are not the same
contracts. The top tiers held **51, 68, 18, 19 and 18**, and I printed one n row.

Held to one declared grouping, cut on the candidate's forecast and joined by contract id:

| forecast | below 0 | 0 to 0.5 | 0.5 to 1 | 1 to 2 | 2+ |
|---|---:|---:|---:|---:|---:|
| production's forecast | −0.30 | +0.21 | +0.36 | +1.21 | −3.93 |
| trailing blend | −0.49 | +0.25 | +0.50 | +0.84 | −4.55 |
| calibrated + aging + participation | −0.42 | +0.10 | +0.63 | +1.76 | −1.74 |
| the adopted candidate | −0.41 | +0.10 | +0.56 | +1.66 | −1.80 |

**Every group keeps its sign in all five columns and the ordering is identical in all five.** No
reversal anywhere, including the 0.5-to-1 one I also reported. The corrected answer is the
stronger one, and I had talked myself out of it with a grouping choice.

What the columns really disagree about is **who the stars are**, which is a different and
interesting thing: 51 contracts against 18 for the same population. Contract by contract the
models correlate 0.974 to 0.988 and agree on sign for 88 to 97%.

The magnitude is still open — the top group runs −1.74 to −4.55 on 18 contracts with no
uncertainty estimate — and that is the honest residual.

### The participation test changed two things

I used `A1Calibrated3Aging` as the no-participation column, believing it isolated participation.
It also swaps the imputed survivorship aging for the uncorrected curve. The clean version pins the
probability of playing to one and holds everything else:

| fixed group | baseline | pinned, currency refitted | pinned, currency held |
|---|---:|---:|---:|
| 2+ | −1.736 | −1.910 | −1.592 |

Signs and ordering survive, so the conclusion holds, but the movement is larger than the −1.74 to
−1.68 I reported and "removing participation barely matters" is withdrawn. So is the broader claim
that calibration errors are not what threatens the conclusion, which the confounded pair never
supported.

### I blamed the wrong stage for the missing contracts

I said the losses were long deals the forecast could not reach, and proposed extending its reach.
The attachment path already extrapolates past its fitted horizons. The accounting: 1,896 eligible,
438 lost at attachment, **241 lost because the signing quarter had too few earlier signings to fit
a price line**, 1,217 priced. Of 35 excluded six-year deals, 34 are lost at the price fit.
Extending the forecast restores none of them. I also read 842 rejections from the full
3,519-contract input as if they were development losses; only 438 are.

### And three smaller ones

The production column is production's forecast priced through **this tree's** currency, not the
production contract-NPV chain's output, and the adapter inherits production's full-panel aging
fit. Calling it "how the live chain compares" oversold it.

The "framing is worth three to five times any modelling difference" ratio had a denominator that
was itself a change of population. On the fixed top group the term contrast is $8.76M to $9.70M
across all five forecasts — large, real, and a contrast between two declared framings rather than
a measure of model uncertainty.

And the instruction I wrote into the queue — cite the three stable categories, drop the elite one
— is the selection problem this project exists to avoid. Withdrawn. The back-test evaluates the
predefined categories and reports where conclusions are sensitive.

## What the run now supports

A fixed set of contracts keeps its valuation ordering under five forecasts including production's
own, and under a participation alternative. That is a robustness check and it passed. It is not a
back-test, it is not evidence about performance, and it is not evidence of systematic trade
mispricing. Those three stay apart in the write-up.

---

## The reviewer's closure record (merged from main)

Thomas asked whether the corrected Claude report at e14e873 means the project is ready
to move on. Fetched and created isolated 50_REBUILD/output/valuation_repair_review.
Inspected the runner and revised report, then executed the full revised valuation runner
using local source inputs. An independent CSV check confirms identical unique contract
IDs, costs and fixed groups across five columns, all 1,217 rows per column, and group
means matching the preceding independent audit within 1e-8 million dollars. All group
signs and group-average rankings agree. Top-group participation, sample attrition and
term-framing figures reproduce.

Closed this review stage and recommended proceeding to remaining development. The known
component variant failure needs repair before using that variant; simulation, valuation
integration and reconciliation, and predefined back-test work remain. Group agreement is
a development robustness result, not realized performance or evidence of trade mispricing.
The grouping was fixed for this corrected comparison after earlier development inspection,
not preregistered before all results were seen.

Added 50_REBUILD/docs/Valuation_Review_Closure_Codex.md and updated the four state files.
Full repair suite was not repeated: it passed 22/22 in the preceding review and this change
only alters the diagnostic runner/report. Targeted runner and independent checks passed.
No candidate implementation merged, no production/source data changed and no reserved
valuation result scored. Local evidence stays under ignored output. Closure records are
committed and pushed together under the repository session-close protocol.

---

## After the closure: the component variant, and what it was really about

The reviewer closed the coverage-and-valuation review and listed what remains. First item: repair
the component model's initialisation error before that variant is used anywhere.

**The cause is the one this codebase keeps warning about.** `A2PerComponentWindow` has to override
the training-pair builder, because its anchors are built per component rather than on the total.
The override was written by copying the base builder and changing the anchor construction — and
the copy dropped the last three lines, the ones that record which horizons the fit actually
managed. The class then read `fitted_horizons_` a moment later and raised `AttributeError` inside
its own fit, before it could answer a single question.

Two files in this tree already carry comments about a second copy of a rule drifting from the
first, and one of them says the project has been bitten twice. Adding the three lines back to the
override would have made a third copy. They now live in one `_record_fitted_horizons` on the base
class and both builders call it.

`BaseModel.fit` also sets `fitted_horizons_ = None` up front now. None was already the convention —
`_guard_horizons` reads it through a `getattr` default and it means "fitted nothing, so no horizon
is out of range", which is true of the flat benchmark and of the production adapter. Leaving the
attribute absent meant a model that *should* have recorded a range failed with an AttributeError
wherever it was next read rather than where the cause was. Making it explicit costs nothing: both
unrestricted models already resolved to None, and the uncertainty run reproduces to the digit
after the change.

**The check that should have caught it.** The reviewer's sharper point was that 22 checks passed
while a registered candidate could not be run at all — those were checks of the models somebody
happened to use. Check 23 now fits every model class in `ability_forecast.py` that carries its own
name and holds the answer to the harness's grid contract: the complete requested grid, nothing
missing, probabilities and shares in range. **All 34 run.** It does not test whether a variant is
any good, which is the bake-off's job. It tests that it runs, which nothing was testing.

It found a second case immediately — the flat benchmark — and that one turned out to be the
legitimate unrestricted convention rather than a defect, which is why the check now reads the
convention instead of assuming the attribute.

Suite: **23 passed, 0 skipped, 0 failed.**

## What remains, in the reviewer's order

The joint simulation on the fitted spread, with the zero-uncertainty identity it already satisfies
and the distribution-mean guard it now has. Then valuation integration and contract-by-contract
dollar reconciliation. Then the back-test, with the grouping rule and evaluation protocol declared
in advance, all predefined categories evaluated and sensitivity reported, and the reserved
evaluation sealed until those choices are fixed.

One distinction to keep, and it is the reviewer's: the ranking of **group averages** is stable
across forecasts. Individual contract rankings are not necessarily identical, and nothing tested
them.

---

## Phase 5: the joint simulation

The thing the point valuation cannot do, stated plainly: it takes one forecast per season,
averages them, and pushes the average through the price line. That is the value of the average
path. A contract is worth the average of the values, and the two differ whenever the price of a
path is not straight in the production on it.

It is not straight, and both reasons push the same way. A club cannot pay below the league
minimum, so a collapsed season costs the same as a poor one. And the price line is itself censored
at that floor, so it bends near the bottom. Convex from below, both of them, so the average of the
values sits above the value of the average.

### The parameter nobody had measured

If a forecast's misses were independent across seasons, a six-year deal would average them out and
its spread would scale as the square root of the term. If a miss persisted entirely, it would
scale with the term. Everything about how uncertain a long contract is turns on which, and it had
never been looked at.

Measured on the same replayed misses the band is fitted on: adjacent seasons correlate **0.43**,
decaying to about 0.26 and flattening. Fitted as a permanent part plus a fading one, that is
**0.253 permanent plus 0.263 fading at 0.66 a season**. Some of being wrong about a player is a
standing misjudgement that does not go away. Some is form or a role or an injury and it passes.
Neither dominates, which is the least convenient answer and the one the data gives.

### Keeping the marginals

The dependence goes in through a Gaussian copula: correlated normals, through the normal CDF to
uniforms, then through the empirical shape the interval layer already fitted. That leaves each
season's own distribution exactly as it was — the one whose coverage has been measured and
reviewed twice — and changes only how the seasons move together. Adding a shared shock instead
would have changed both at once, and the marginal it would have changed is the one with evidence
behind it.

Participation is an absorbing exit whose conditional survival is the ratio of consecutive
marginals, so the model's own probabilities come back exactly while an exit sticks. The zeros
arrive together now instead of being smeared across every season as a probability.

### One standing flag resolved by not doing what it asked

The flag said the band was on the season total only, so the rate and the games share needed
separate bands before the simulation could draw them jointly. They do not. The quantity with a
fitted spread is the season total *given he played*, and its miss already contains both — the
player who was healthy but worse and the one who was as good but hurt are both in there.
Splitting them would need two spreads where the data supports one. The joint object the simulation
actually needs is (participation, conditional season total), and that is what it draws.

### The identity

With the spread set to nothing and participation certain, every path is the same path, so the
simulation has to return the point valuation exactly. **300 real contracts, largest gap
$0.000000.** That is the guard the plan asks for in place of the retired k=0 identity, and it is
checked on real contracts rather than a constructed case.

### What came out

Averaging path values rather than valuing the average path is worth **+$0.18M on the average
contract** — and it lands exactly where the mechanism says it should. +$0.23M on one-year deals,
+$0.22M for players forecast between nothing and half a win, falling to nothing for stars and long
terms whose paths never come near the floor. The average production per season is identical either
way (0.3491 against 0.3487), so the whole of the difference is curvature and the floor. **No sign
changes anywhere**, which is consistent with the robustness work.

And the spread, which did not exist before. An eight-year deal has a standard deviation of
**$11.3M** around a mean of $5.3M, and a **38% chance of losing money**. A club signing one is not
buying a number.

### One claim I nearly published without measuring it

The first version of the run log said the spread grows faster than the square root of the term
"because a misjudgement does not wash out". That does not follow from the table: long deals go to
better players with wider bands, so a spread growing with term says nothing on its own about
persistence. Replaced with the counterfactual — the same contracts redrawn with independent
seasons. **Persistence widens a seven-year deal's spread by 44%.** The one-year row comes back at
exactly 0%, which is the check: with a single season there is nothing to correlate and the two
draws must agree.

Also dropped a percentage column. The point surplus averages near zero by construction, so a gap
expressed as a share of it read in the hundreds of percent and meant nothing.

### And a tolerance that was a magic number

Check 24's marginal test used a flat tolerance that passed at four thousand paths and failed at
three thousand — not because anything was wrong, but because the ninetieth percentile of a
long-tailed shape is the noisiest thing in the test. A constant tolerance on a Monte Carlo quantity
is a check that passes or fails on the draw count. It now scales as one over the square root of the
sample, and passes at 1,500, 3,000 and 8,000.

Suite: **24 passed, 0 skipped, 0 failed.**

### Absent, and stated

The RFA walk-away on the path — a restricted player whose value collapses can be walked away from,
which truncates the club's downside again, so the lower tail here is too heavy. The rebuild tree
has no terminal-value machinery yet. Returns after a missed season, though no term in this sample
asks for one. Goalies, as everywhere.

Next: valuation integration and contract-by-contract dollar reconciliation, then the back-test with
its grouping rule and evaluation protocol declared in advance.

## Phase 5 simulation review

Thomas requested review of 95750f5 to determine whether Phase 5 could close. The first
fetch approval timed out; the permitted retry succeeded. Isolated the candidate under
50_REBUILD/output/simulation_review and ran its complete simulation and repair suite.
24/24 checks pass, including all 34 variants on page 2018. Independent deterministic
pricing agrees with ProductionCurrency.value on 300 contracts within $7.45e-09.

Captured per-page calibrators: the runner instead uses the 2025 shape and persistence
for all 1,217 development contracts, incorporating residual outcomes through 2024.
The persistence fit uses Spearman correlations as Gaussian correlations. A 600,000-path
audit requests 0.42658 and gets 0.40927; the candidate's own larger-sample self-test fails
(0.318 versus 0.334). Its ordinary 3,000-path test is too loose to detect the mismatch.
Absorbing exits preserve decreasing marginals but omit possible returns; the zero-rising
count cannot justify saying nothing is lost. Remaining Phase 5 scope is recorded.

Simulation means broadly reproduce but some exact reported Monte Carlo values differ.
Mean surplus uplift is $0.188096M; 229 individual contracts change sign. The one-year
standard-deviation contrast is -0.0486%, rounding to zero rather than exactly zero.
The independent-season arm retains absorbing participation and only removes residual
dependence. Added NPV_Simulation_Review_Codex.md and review_npv_simulation.py and updated
the four state files. Phase 5 remains open pending dated calibration, dependence correction
and a return-capable path or explicit scope decision, plus remaining integration gates.

No candidate model fixes or merge. The candidate run itself used later residual outcomes;
this is the finding, not a claim of clean historical validation. No new reserved-cohort
trade valuation evaluation was run. Existing unrelated untracked documents and env were
left untouched. Generated evidence stays ignored; review/state records committed together.
---

## The Phase 5 review: a look-ahead I built a battery to catch

Two implementation defects and one scope claim. All three correct, all three fixed, and the first
is the one worth sitting with.

### The runner used 2025's calibration for contracts signed in 2015

`per_season` fits a banded model per page and returns the page calibrators. I then fitted **one**
more, on the latest page in the whole contract input — 2025, whose replay contains outcomes through
2024 — and used its residual shape and its persistence for every contract. All 1,217 are earlier.

The forecasts were dated at the signing. The price lines were rolling. The uncertainty around
them, which is the **only** part of this chain that reads outcomes, was not. And it is not a
harmless reuse of the same numbers: fitted on what 2015 could see, the permanent part of a miss is
0.00; on 2025 it is 0.25.

There is a whole leakage battery in this tree, and it could not have caught this. It tests the
forecast and it tests the band; the defect was in the runner that consumes them. So check 25 now
corrupts every season at or after the decision date and requires the shape, the scale, the
persistence and the return rate to come back identical to the last digit. It passes, and page
2018's parameters reproduce the reviewer's figures exactly.

What the fix revealed is worth recording on its own: the permanent/fading **split** moves enormously
across pages while the **total** adjacent correlation barely moves at all, 0.39 to 0.43 throughout.
An early page's replay cannot reach far enough to tell a standing misjudgement from one that fades.
So the split is a parameter of a chosen dependence model, not a measured fraction of every mistake,
and the report now says so.

### The copula imposed a correlation I never asked for

I measured Spearman rank correlations — right, for a long-tailed shape — and then handed those
numbers straight to the normal draws as though the two scales were the same. They are not. A
Gaussian copula with latent correlation r delivers rank correlation (6/π)·arcsin(r/2), always a
little below r. Asking for 0.427 delivered 0.410.

Fixed by inverting. The important part is the test: my Monte Carlo check could not see a
seventeen-thousandth discrepancy at three thousand paths, and the reviewer found it by running my
own self test at six hundred thousand. **The relationship is now checked in closed form**, where
the error is either there or it is not, with the simulated check kept as a sanity test at a
tolerance derived from a rank correlation's sampling error. Reverting the conversion fails the
analytic test at every sample size.

### Returns: my argument was a non-sequitur

I made an absence permanent and justified it by observing that no term in the sample asks for a
probability of playing that **rises**. The reviewer's counterexample settles it in one line: 60%
play both seasons, 30% only the first, 10% only the second — the marginal falls from 90% to 70%
and one path in ten is a man coming back. A falling marginal says nothing about returns.

Now a two-state chain: a return rate estimated from seasons before each decision date, about 0.10,
and the exit probability solved each season so the model's marginals still come back exactly. The
absorbing version sits beside it as the sensitivity it always was. It costs little — $11.02M
against $11.09M of within-contract standard deviation on the eight-year cohort — but that is a
measurement now rather than an assertion, which is the whole point.

### Reporting, corrected

"No sign changes anywhere" was false: **228 of 1,217 contracts change sign**. I read it off the
tier means, which do keep their signs. Those are two different statements.

The "independent seasons" arm keeps participation correlated in both arms, so it isolates the
conditional performance error and is not an independence counterfactual. A point valuation does
**not** assume independent errors — an expectation averages under any dependence; what dependence
changes is the spread and the value of a path once the price line stops being straight. The league
minimum is the convexity; the tobit prediction is linear, so crediting the price line with bending
was wrong. The one-year 0% was two random samples rounding to agreement and now runs on common
draws. The identity is checked against `ProductionCurrency.value` rather than my own helper on both
sides, which would have passed with a shared pricing bug in it. And the previous report mixed
figures from two runs; everything now comes from one.

### And the scope claim

"Phase 5 built" overstated it. This is an **aggregate contract simulator** for the current price
interface. The RFA walk-away and control years are absent — which makes the lower tails too heavy,
and does not mean every signed-contract downside was one the club could have avoided — along with
goalies and contract-by-contract dollar reconciliation. Eight claims are withdrawn in the report's
section 8.

Suite: **25 passed, 0 skipped, 0 failed.**

## Simulation repair verification

Reviewed Claude's 8239d29 in a detached ignored checkout. Full runner and 25/25 checks
passed. Reproduced 228 sign changes, $188,119 mean uplift, page-2018 persistence and
$11.023773M versus $11.091201M eight-year spread. Independent 600,000-path check
passes; deliberately restoring the old rank conversion fails the analytic guard.
The deterministic identity against ProductionCurrency.value differs by $7.45e-09.

Date selection is fixed in the actual runner, but c25 reconstructs calibration and
never calls its consumer; it passes even with main/page_dependence disabled. The
common-draw comparison only shares normals, not participation uniforms, so all 605
one-year standard deviations differ and the cohort contrast is -0.1056556%, not exact
zero. Report two focused corrections, with continued integration allowed. The two
clipped marginal cases deviate by only 0.045 and 0.381 percentage points. Return rate
homogeneity remains an explicit prototype assumption. Full Phase 5 scope remains open.

Added NPV_Simulation_Repair_Verification_Codex.md and review_simulation_repairs.py,
updated all four state records. No model fixes or merge, no production/source writes,
no reserved trade valuation. Unrelated untracked documents and env preserved.
Generated evidence ignored. Review and state files committed and pushed together.
---

## The repair verification: two corrections to the repairs

Both correct, both small, both about the same thing — a claim that a test was doing something it
was not.

### The common draws were half common

I built shared participation uniforms, passed only the performance normals to `draw_paths`, and
used the uniforms afterwards for nothing but a clipping count. So performance was shared across
arms and participation was redrawn in each of them.

The tell was in my own output. I reported the one-season dependence contrast as 0%, which I
described as exact on common draws. It was −0.106% rounding to zero, and **all 605 one-season
contracts had different standard deviations between the arms**. A one-season term has no
dependence to impose; if the draws were shared the two arms would be the same path.

Fixed. `draw_paths` takes the uniforms, the runner passes them to all three arms, and the
one-season case is now **asserted path by path** instead of printed: 605 of 605 identical, contrast
0.00000000%. The returns-against-absorbing pair reads $11.01M against $11.08M once the noise is
out, where it read $11.02M against $11.09M with the noise still in it — and that difference was
never separated from sampling error before.

### The leakage guard tested everything except the thing that broke

Check 25 built its own correctly-dated calibrator and compared it before and after corrupting the
future. That tests the calibration. The defect was in **selection** — the runner reaching for the
latest page's calibration for every contract — and the check never touched it.

The reviewer proved it the right way: they replaced the runner's `main` and `page_dependence` with
functions that raise, and the check still passed.

So selection is now a named function, `calibration_for`, that the runner calls and the guard drives.
The check runs `per_season` and `page_dependence` on a sample spanning two pages, and asserts three
things: that more than one page is in play, that scrambling the future leaves the early contract's
paths bit-identical, and — the one that gives it teeth — that the two pages give **different**
answers, so a revert to `max(spreads)` cannot pass unnoticed.

Verified both ways. Stubbing `page_dependence` fails it. Restoring latest-page selection fails it.

### And the comments

The report had withdrawn absorbing exits, a bending tobit prediction and the independent-error
framing; `npv_simulation.py` still described all three in its own docstring. A report that
contradicts the file it documents is a trap for the next reader. Brought into line.

### Carried as prototype limitations

The return rate is estimated from recent absences and then applied to every absent state, whatever
the player's age or however long he has been out. That is a simplifying assumption, not a return
law. Two contracts need an exit probability clipped to hold their marginal, missing target by 0.045
and 0.381 of a percentage point.

Suite: **25 passed, 0 skipped, 0 failed.** Phase 5 stays open: the walk-away option and control
years, goalies, the plan's joint path design, development dollar scoring, and contract-by-contract
reconciliation.
