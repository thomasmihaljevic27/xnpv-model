# Session 2026-09-24 — status-adoption review closed; reporting corrections

The closure review of `5864cb7` (`3542bd3`, merged; state-file conflicts kept both sides) closed the
contract-status adoption review: 45/45, valuation chain agrees exactly on 1,217 contracts, leakage
diagnostic completes, carry-forward result reproduces. Provisional status-only leader retained;
carried version kept as a tested sensitivity. Proceed to the star forecast residual.

**Reporting corrections applied** (`Skater_Contract_Test.md`, the state files, the 2026-09-23 log):
- the 29 group changes are 28 up and 1 down, not "all upward" (the doc listed the one downward move
  and still said "all upward");
- the 0.000744 WAR clipping average is among the 18 affected terms, not across 1,473;
- the carried sensitivity moves the whole last supported participation fit, so its loss rejects that
  method, not every extension of the status effect, and does not show the cliff is realistic.

**CLAUDE.md:** two rules, on universal words in summaries and on letting one failed variant stand for
the idea.

**Still open:** matched scoring of the simulated value distributions (old against new leader); Phase 5
as a whole.

**Next:** the star forecast residual.

---

# Session 2026-09-24b — the star residual, located

**Located before designing anything.** From the adopted leader's harness rows, three-win-and-up
tier: rate per 82 predicted 3.24 against 3.26 at the valuation season, then 2.99/3.26, 2.47/3.10,
1.90/2.82 at one, three and five seasons out. The forecast walks stars down ~0.27 a season; stars
who played lost ~0.09. Every tier's walk is too steep, smaller. So the hinge repair (valuation-season
fit) could not reach it. The Phase 3 record shows the walk replaced per-horizon regressions for
structural reasons when participation was still a placeholder.

**Three single changes** (declared in `run_star_residual.py` before running): survivors-only aging
curve; curve without level terms; per-season regression. Results:
- pooled season WAR squared error: 1601, 1130, 1288 of 2,000 lower than adopted (none separates);
  absolute error worse for all three;
- star tier: no level terms takes the five-season rate miss −0.92 -> −0.26, tier error lower in
  1975/2000; survivors-only −0.83 (1998/2000); per-season regression −0.68 (546/2000);
- but no level terms walks the lowest tiers down too fast (below 0: −0.07 -> −0.36);
- dollars: 1065–1212/2000 on the adopted line, 449–734 on the per-season regression's line.

Nothing adopted; no combination (no single improved a primary score). The level slope is doing two
jobs. Next: a level effect that differs by tier (e.g. a hinge in the aging curve's level term, or a
multi-season level), scored the same way.

**Files:** `ability_forecast.py` v2.2; `run_star_residual.py` v1.0 (new);
`50_REBUILD/docs/Star_Residual.md` (new); the four state files.

---

# Session 2026-09-24c — star residual review applied; matched comparison; a second level slope

Review `e7a4979` (merged; state-file conflicts kept both sides): keep the adopted model; the no-level
experiment changed the curve's sample as well as its formula (7,164 -> 9,459 rows, 2021 page); add a
matched comparison and a rows-and-weights check; then the next aging candidate.

- Confirmed in `aging_additive.fit`: pairs without a prior season have a missing lagged level and
  the finite-value filter drops them; the comment claiming they were "fitted on age and position
  alone" was wrong. Corrected.
- Added `sample="lagged"`, an optional level knot, and a fit fingerprint (rows by player, season,
  observed/imputed, with weights). Check 46: matched and hinge fits share the adopted curve's
  fingerprint through the real model classes; the unmatched one does not; the adopted curve's
  coefficients unchanged. A mutant (matched on its own rows) fails. Suite 46/46.
- v1.1 run: matched no-level reproduces the review exactly (−0.328 / −0.351 / 0.8133); pooled
  1610/2000, dollars 1531 and 1473/2000, MAE worse. A second level slope above 2 wins does nothing
  for stars (−0.925) and loses (263/2000; dollars 180/203). Neither adopted.
- Diagnostic (2021 page): the curve fits one-season changes by prior-season rate (3+: −0.226 observed
  vs −0.270 fitted); by the starting season's rate the same pairs show −1.149 (mean reversion). The
  forecast applies a single-season-level slope to a shrunk multi-season rating; hypothesis: stars are
  pulled back twice. Next candidate: level measured on a multi-season rate, same rows.

Files: `aging_additive.py` v1.1, `ability_forecast.py` v2.3, `repair_checks.py` v3.4,
`run_star_residual.py` v1.1, `Star_Residual.md`, `CLAUDE.md`, the four state files.

---

# Session 2026-09-24 - matched-aging review completed

Continued the review of candidate 670b614 begun September 23. The full star
runner completed successfully: 40,510 forecasts per variant, 1,217 contracts
priced, 1,176 ended terms scored. Matched/hinge season-score win counts reproduce
1610/263 of 2000 exactly; primary dollars reproduce 1531/180. Primary dollar RMSE
is 3.513M adopted, 3.487M matched, 3.519M hinge. Hinge-currency counts are
1117/1474/200 against published 1118/1473/203 for unmatched/matched/hinge; numerical
cause not isolated, conclusions unchanged.

Final row audit passes: participation, games share and h0 rates unchanged across
all variants; adopted forecasts exactly match the preceding candidate on all
40,510 rows; matched model agrees with independent review implementation within
1e-12. Full suite 46/46 and independent sample/weight mutations were completed
September 23. Review closes the sample-matching repair. Proceed with the proposed
multi-season input experiment, retaining the adopted baseline and currency;
double mean reversion remains a hypothesis. The failed fixed-knot hinge does not
reject every nonlinear level effect. Stale docstrings/file listings can be
corrected alongside the next experiment. No implementation adoption or candidate
merge. Full simulation/production chain and confirmatory cohorts were not rerun.

Review and helper in 50_REBUILD; logs and generated tables remain ignored.
Updated four state records and this session log for closure.

---

# Session 2026-09-24d — the multi-season aging level, scored

Review `82e9db8` (merged) closed the sample-matching repair and asked for the multi-season-level
candidate with rows, weights and the primary dollar currency held, plus stale-description cleanup.

- Cleanup: aging docstring (unlagged pairs are dropped, not fitted on age alone; lagged noise is not
  independent by construction), runner's 1.06 figure (0.866 over every forecast; 1.06 among seasons
  played), report file list and "one change" wording, hinge wording (rejects that construction only).
- Built `multi` level mode (weighted t-1..t-3, t-1 required) and `A1StatusAgingMultiLevel`; check 46
  covers it (same fingerprint). Suite 46/46.
- Before the run: the multi-season level steepens the curve's level slope (−0.071 vs −0.062 per win),
  the opposite of the "pulled back twice" hypothesis; recorded the prediction of no star gain.
- Result: stars −0.956 (adopted −0.921), star tier 1/2000, pooled 234/2000, dollars 826/2000 and
  778/2000. Not adopted. [Corrected after review: this rejects that repair, not double shrinkage as a mechanism.]
- Open: why the walk drops stars ~0.27 a season while the forecast's stars lost ~0.09; the curve's
  training pairs and the forecast's star tier are different populations.

Files: `aging_additive.py` v1.2, `ability_forecast.py` v2.4, `repair_checks.py` v3.5,
`run_star_residual.py` v1.2, `Star_Residual.md`, the four state files.

---

## Multi-season aging candidate review

Reviewed 3ba71cd (model/report 7bff643) in isolated star_multi_review checkout.
Full suite passes 46/46. Complete v1.2 run reproduces star h5 bias -0.956,
lowest tier -0.025; 1/2000 star, 234/2000 pooled season, 826/2000 main dollar,
778/2000 sensitivity dollar wins. 40,510 forecasts per variant, 1,217 priced,
1,176 ended terms scored. Adopted forecast exactly unchanged; participation,
games share and h0 rates identical across variants; matched reference reproduces
independent implementation within 1e-12.

Independent tests cover weighted arithmetic, a missing intermediate season,
required t-1, non-default row indices and ignored future values. On all seven
pages, multi and adopted use identical rows/weights, adopted coefficients match
670b614 exactly, and future-rate corruption leaves multi coefficients unchanged.
Age-27 2021 coefficients -0.061921 vs -0.071153; steps -0.250787 vs -0.265415.

No new adoption. Proceed with a bounded diagnostic, correcting the group rule:
harness stars use a 60/40 two-season total with fallbacks, not three-season
weighted totals. Freeze page fits and compare lagged observed inputs, current
observed inputs and recursive projected inputs on common player/page/transitions.
The first aging step is page to page+1, not the preceding season to the initial
calibrated forecast. Keep hindsight/conditional-rate diagnostics distinct from
full-cohort season-WAR scoring. One-step success/failure cannot uniquely identify
recursion versus selection, and the failed substitution does not reject all
forms of double shrinkage. Prior cleanup substantially applied. Full simulation,
control years, production reconciliation and confirmatory cohorts not rerun.

Report and helper under 50_REBUILD; generated evidence ignored. Updated state
and session records. No candidate merge, production change or vendor-data write.

---

# Session 2026-09-24e — the star walk diagnostic

Review `a9e039c` (merged; conflicts kept both sides): the multi-season candidate reproduces, keep
the adopted model; correct the cohort description (harness tier is a 60/40 two-season total with
fallbacks); don't treat the failed substitution as rejecting double shrinkage; run the diagnostic
as specified (frozen fits; training-aligned, current-level and forecast-path arms on common rows;
hindsight labelled; comparison groups; no binary verdict).

- Corrections applied in `Star_Residual.md`, the state files and the log; two CLAUDE.md additions.
- `run_star_walk_diagnostic.py` v1.0: 7 pages, 27–32 tier members a page, 876 common transition rows.
  Observed −0.112 a season (+0.003, −0.045, −0.119, −0.209, −0.228 by transition); arm 1 −0.352,
  arm 2 −0.328, arm 3 −0.279; starting level error −0.039, accumulating to −1.055; survivors-only
  curve −0.307. Comparison groups: 2-to-3 tier −0.189 vs −0.226; realised t−1 rate ≥3 −0.216 vs
  −0.344.
- Localisation: the curve's step is too steep for stars out of sample on realised inputs of either
  timing; recursion does not add to it; imputation is a small part; membership matters in part.
  Not shown: why a curve matching its training pairs over-predicts the same kind of group afterwards
  (different seasons); survivorship in the rows.

Files: `run_star_walk_diagnostic.py` (new), `Star_Residual.md`, `CLAUDE.md`, the four state files.

---

## Star-walk diagnostic review

Reviewed 55f23cb in isolated star_walk_review checkout. Regenerated 40,510-row
adopted harness output before the diagnostic. Published 876-row transition table,
comparison groups and full-cohort table reproduce. Independent audit reconstructs
membership and all row arithmetic, weights, level errors and frozen curves;
projected step agrees with direct step to 4.72e-16. Every arm is finite and common
on this run. Counts: 84 careers, 492 distinct player-season transitions. Attrition
983 calendar-observable -> 894 three played -> 883 three >=10 GP -> 876 with age.
Career bootstrap (2000, seed 20260924) keeps negative error intervals for all
four arms: prior [-.347,-.130], current [-.325,-.103], path [-.264,-.067], survivor
[-.299,-.090]. All seven pages' prior-input mean errors are negative.

Accept diagnostic and proceed with a declared sustained-quality candidate.
Observed-input excess decline establishes that recursion is not required for
this selected-sample miss, not that all pipeline or selection issues are excluded.
Historical survivor training does not remove selection of later star survivors.
Keep candidate matched rows/weights, all-tier scores and adopted primary currency.
Existing model/suite unchanged from independently verified 3ba71cd 46/46; did not
rerun full suite, which does not exercise new diagnostic. New runner audited
separately. Recommend retaining common finite-arm assertions because wmean drops
missing entries per column. Full valuation/simulation and confirmatory cohorts
not rerun. No candidate merge, adoption, production or vendor-data writes.
Report/helper under 50_REBUILD; generated evidence ignored; state records updated.

---

# Session 2026-09-24f — a sustained-quality aging level, scored

Review `6a14784` (merged): the diagnostic reproduces; it supports another aging candidate but only
localises (84 players, 492 distinct transitions over 876 rows; career-resampled excess −0.240
[−0.347, −0.130]; survivor selection remains). Proceed with a clearly specified sustained-quality
candidate, rows/weights/currency held, every tier and horizon scored.

- Write-up qualified accordingly; CLAUDE.md rule on independent units.
- Candidate: min(rate t−1, rate t−2) as a second level in the aging curve (t−1 alone where t−2
  missing); same rows and weights (check 46); walk passes the projected level as both.
- Before the run: sustained slope −0.034 per win (high-in-both-seasons players declined more in the
  training pairs); 3.2-win 27-year-old's step −0.280 vs −0.251; predicted no star gain.
- Result: pooled 0/2000, star tier 0/2000, dollars 42/2000 and 85/2000, stars −1.017 five out; every
  tier more negative. Not adopted.
- Every-tier table (adopted): every tier's miss drifts down with the horizon (−0.19 to −0.34 below
  the top, −0.89 for stars). Four level constructions have not helped; the two with steadier levels
  found steeper level effects in the training pairs. Untested reading: older training seasons differ
  from the seasons the curve is applied to; a recent-seasons curve would test it.

Suite 46/46 on this code. Files: `aging_additive.py` v1.3, `ability_forecast.py` v2.5, `repair_checks.py` v3.6,
`run_star_residual.py` v1.3, `Star_Residual.md`, `CLAUDE.md`, the four state files.

---

## Sustained-quality aging review

Reviewed f83240a (results 81d885f) in isolated star_sustained_review checkout.
Full suite 46/46. Complete v1.3 scoring reproduces 0/2000 pooled and star
squared-error wins, -1.017 star h5 rate bias, 42/2000 main-dollar and 85/2000
sensitivity-dollar wins on 1,176 ended terms (1,217 priced). Adopted forecasts
exactly unchanged on all 40,510 rows; participation/games/h0 rates identical.
Independent input tests cover min of prior rates, single-season fallback,
required prior season, future data, row index, and duplicated projected inputs.
All seven aging fits preserve rows/weights; adopted coefficients match prior
code exactly; future corruption leaves sustained fits unchanged.

Corrected blanket loss claim: season MAE 0.4564566 -> 0.4560089, 1897/2000 wins,
95% difference interval [-.000990,+.000098]; Brier unchanged. Primary decision
remains reject. Horizon endpoint drift reproduces but is not monotonic for the
2-to-3 tier and does not identify older/newer-season change. Prediction passes
one projected level into both inputs; only the declared construction is tested.
Recommend one fixed five-season-half-life sensitivity preserving rows/targets
while deliberately changing weights with an asserted dated factor. Keep existing
matching guards for other classes, same formula and currency, no half-life sweep.
If it fails, retain the quantified limitation absent more specific new evidence.
Next experiment not implemented. No candidate merge, adoption, production or
vendor-data writes. Report/helper under 50_REBUILD; output ignored. State updated.
Full simulation integration and confirmatory cohorts not rerun; Phase 5 open.

---

# Session 2026-09-24g — one recency-weighted curve; the star residual kept as a limitation

Review `ebc9f9b` (merged): sustained-level results reproduce; corrections: absolute error improved
slightly (not "worse on every score"), the horizon pattern does not establish older seasons as the
cause, the sustained construction never carries observed consistency forward. Recommended one
predeclared recency weighting (half weight per five seasons of age, all rows kept), then retain the
bias as a limitation if it fails.

- Corrections applied in `Star_Residual.md`; CLAUDE.md: recurrence noted on the universal-word rule;
  new rule against reading a cause off a horizon pattern.
- Built `recency_halflife` and `A1StatusAgingRecency`; check 46 now asserts rows unchanged and weights
  exactly the adopted weights × 0.5^(age/5) (half-life-4 mutant fails). Suite 46/46.
- Before the run: the 2021 curve is slightly gentler (3.2-win 27-year-old −0.230 vs −0.251); predicted
  a small star gain. Wrong: stars −0.947 (adopted −0.921); pooled 69/2000; star tier 463/2000; dollars
  155/2000 and 116/2000; MAE slightly worse; below-replacement tier slightly better five out, the other
  four worse. Not adopted.
- Star residual closed as a stated, quantified limitation (standing flag added). Eight constructions
  scored, none separating on the primary scores.

Files: `aging_additive.py` v1.4, `ability_forecast.py` v2.6, `repair_checks.py` v3.7,
`run_star_residual.py` v1.4, `Star_Residual.md`, `CLAUDE.md`, the four state files.

---

# Session 2026-09-24h — what counts as a star

Thomas: the "star" cut-off (3+ WAR) was never tested before the star work; a tested measuring stick
would be good, though he did not expect it to change anything.

- Declared before running: fixed cut-offs 2.0–4.0 on the harness's 60/40 two-season total; top 2%,
  5%, 10% of each page; top 5% by 60/40 rate per 82 (60/40 games ≥ 40). "Makes a difference" to a
  candidate: its star-group share crosses 50% or moves into/out of ≥1950 or ≤50 of 2,000.
- Checked first: WAR totals are already prorated to 82 games for the shortened seasons (D20 in
  `player_season_table`), so a fixed cut-off is comparable across pages.
- Results (`run_star_definition.py`): three wins = 27–32 of ~990 per page (~top 3%); top 5% starts at
  2.37–2.64, top 2% at 3.23–3.66. With no cut-off the miss grows gradually to the 95th percentile and
  steps up above it (five-season rate −0.05 to −0.22 → −0.52 → −1.04). Headline size runs −0.50 (top
  10%) to −1.18 (4.0+); direction unchanged. Six of eight candidates unchanged; unmatched no-level
  crosses the decisive band at 3.5+/4.0+; the second level slope's star gain depends on the
  definition (hinge at 2 wins). No adoption decision depends on it.
- Recommended: keep three wins; report top 5% and top 2% beside every star figure. Pending Thomas.
  Not tested: a market (cap-share) definition for star-contract claims.

Files: `run_star_definition.py` (new), `Star_Definition.md` (new), the four state files.

---

## Recency-weighted aging review

Reviewed 293f4d6 in isolated star_recency_review checkout. Complete scoring
and suite reproduce: 46 passed, zero skipped/failed; 69/2000 overall,
463/2000 star, 155/2000 main-dollar and 116/2000 sensitivity-dollar wins.
40,510 forecasts, 1,217 valued contracts, 1,176 ended terms. Baseline unchanged
against prior independently generated forecasts. MAE worsens .456457 -> .457016.
Captured actual least-squares inputs: seven pages preserve rows, targets, and
design while weights change by the exact five-season factor, including imputed
rows. All-ones identity, future corruption, and half-life mutation verified.
The audit helper initially loaded the old module path after importing the
historical implementation; restored candidate path precedence and reran it
successfully. This was a reviewer helper issue, not a candidate defect.

Close the current residual investigation as a limitation, retain adopted.
Correct summary horizon mismatch: -0.170 WAR is h0; h1 is -0.419, h3 -0.643,
h5 -0.866. Keep conditional-survivor and dollar-translation qualifications.
Proceed to Phase 5 matched simulation comparison, development dollar scoring,
and contract reconciliation, with fixed cohorts/common draws/dated fits.
No candidate merge, adoption, production change, or source write. Generated
results ignored; review and helper under 50_REBUILD; state records updated.
Full simulation acceptance and reserved cohorts not rerun in this review.

---

# Session 2026-09-24i — star work closed; star definition decided; on to Phase 5 acceptance

- Thomas: keep it simple; document 3+ wins as a tested, reasonable measuring stick and record the
  results. Done in `Star_Definition.md` and the state files; the proposed top-5%/top-2% reporting
  convention is not adopted.
- Review `d320439` (merged): recency reproduces (69, 463, 155, 116 of 2,000); the investigation closes
  as a measured limitation; no further aging variant without more specific evidence. Corrections
  applied: season-WAR star miss one season out is 0.42, not 0.17; the limitation does not establish a
  per-contract dollar under-valuation, and the diagnostic is conditional on observable consecutive
  seasons. CLAUDE.md rule on copying figures with their labels.
- Next: Phase 5 acceptance — matched simulation-distribution comparison (previous vs adopted leader),
  development dollar scoring, contract-by-contract reconciliation against production.

---

# Session 2026-09-24j — Phase 5 acceptance: skater dollar scoring

- Plan's Phase 5 acceptance: identity (current), dollar scoring on development pages (missing for
  skaters), reconciliation (rebuilt 2026-09-23h). Built the skater dollar scoring with the matched
  previous-vs-adopted comparison the adoption review left open.
- Refactor first: the goalie runner's scoring helpers moved to `dollar_scoring.py`; goalie rerun
  reproduces exactly (log identical after header; 133 scored rows, max diff 0.0). `point_valuation`
  extracted from `run_npv_simulation.main`. Check 47 (scorer refuses a moving target; guard-removed
  mutant fails).
- Results (1,176 ended terms, 767 players, adopted line primary): point RMSE 3.513 vs 3.529 (previous
  lower 1/2000), simulated 3.513 vs 3.526 (15/2000); simulated MAE previous lower 2000/2000 (1.743 vs
  1.758). Calibration: adopted passes the pooled PIT and own-draw coverage tests; previous fails the
  50% band (−5.8 points) and PIT variance (0.091). Sensitivity line agrees.
- Each Phase 5 acceptance item now has a current run on the adopted leader; closure left to review.
  Suite 47/47.

Files: `dollar_scoring.py` (new), `run_skater_dollar_scoring.py` (new), `run_goalie_control_years.py`
v1.5, `run_npv_simulation.py` v2.6, `repair_checks.py` v3.8, `Skater_Dollar_Scoring.md` (new), the four
state files.

---

## Player Model acceptance review

Reviewed 01e78a9 in isolated acceptance_review checkout. Unmodified suite 47/47.
Skater runner: 1,217 priced/simulated, 1,176 ended terms, 767 players. Primary
RMSE/MAE/bias table and resample counts reproduce; some sensitivity RMSE cells
vary by .001M at printed precision. Point values match prior integration within
$1.49e-08. Production reconciliation code unchanged, engine not rerun here.
Initial unmodified skater run was stopped during slow resampling after its
primary table printed. Complete rerun uses verified player-group sums/counts
for the same seeded bootstrap samples; forecast fitting, paths and pricing are
unchanged. Generated evidence is ignored.

Two findings. (1) Shared scorer fixes the first arm's player, years, pricing
cut and dollar factor before comparing targets. Mutated second-arm player and
signing date are accepted despite $2.475M and $26,000 independently computed
target gaps on the check fixture. Prototype identity/date assertions preserve
valid values and catch five mutations. (2) Exact floating-point comparisons in
monetary PIT/interval coverage misclassify 17 tied outcomes per arm; rounding
both draws and outcomes to six decimal dollars preserves the qualitative
calibration result. Contract 4127's PIT moves .544 for a $2.33e-10 difference.
Repairs are bounded scoring work, not new statistical model development.

Recorded user scope clarification in AGENTS and state: close the Player Model
candidate first; picks and prospects are separate work. Final adoption/drop
comes when that work resumes. No candidate merge, adoption, model patch or
vendor-input writes. Review/helper in 50_REBUILD; state/session updated.

Final verification: full goalie runner completed. On all 133 scored contracts,
the old inline scorer and shared scorer are exactly equal on both price lines,
including targets, point values, simulated means, and draw arrays. Real-sample
checks also confirm the accelerated resampling matches the reference routine.

---

# Session 2026-09-24k — acceptance repairs; the scorecard; Phase 6 set aside

- Review `674fe67` (merged): acceptance results reproduce; two scoring defects. Fixed: the shared
  scorer checks identity fields one by one and prices each target from its own row (check 47 now
  mutates player, date, seasons, quarter, RFA status; each refused by name); dollar ties rounded to 6
  decimals (check 48). Both checks fail against the pre-repair behaviour. Skater rerun: valuations
  identical (max diff 0); calibration refreshed to the review's figures. Goalie rerun: identical.
- Thomas: Phase 6 is not part of the rebuild (picks and prospects unfinished); after Phase 5, present
  the overall scorecard between the current model and the candidates for a decision.
- Found while building it: the stress tests' "what the chain does today" is the flat benchmark, not the
  live chain, and my 2026-09-23 summary repeated it as "beats production's forecast". Corrected in the
  runner, the report and the log; CLAUDE.md rule.
- Scorecard (`run_model_scorecard.py`, production chain via `ProductionChain`): season RMSE 0.868 /
  0.815 / 0.816 (current / adopted / previous), candidates better in 2000/2000 and at every horizon;
  Brier 0.231 vs 0.133; current over-forecasts good players (3+ +0.99 at valuation), candidates
  under-forecast stars at long horizons. Dollars on one line: squared error 3.548 / 3.513 / 3.529
  (adopted lower 1237/2000, not decisive), absolute 1.771 / 1.675 / 1.674 (1962/2000), bias −0.14 /
  −0.55 / −0.59. Reconciliation: production values long deals far more negatively (8-year gap +33.8).
- Recommendation: carry the adopted candidate forward. Decision is Thomas's.
  Suite 48/48.

Files: `dollar_scoring.py`, `repair_checks.py` v3.9, `run_skater_dollar_scoring.py` v1.1,
`run_model_scorecard.py` (new), `run_stress_tests.py` v1.3, `run_bakeoff.py`, `Skater_Dollar_Scoring.md`,
`Model_Scorecard.md` (new), `Skater_Contract_Test.md`, `CLAUDE.md`, the four state files.

---

## Player candidate acceptance closure review

Reviewed candidate 7b046a3 in isolated acceptance_closure_review checkout.
Full suite 48/48, no skips. Changed all ten contract identity/pricing fields:
each rejected. Previous scorer accepts the changed-player fixture. Precision
mutation fails check 48. Full scorecard rerun: 40,510 forecasts per model,
1,217 simulated contracts, 1,176 ended contracts / 767 players scored. Prior
acceptance point values, means and targets exactly unchanged. Corrected pooled
PIT and central coverage reproduce. Bootstrap uses previously verified seeded
player-group sums; small dollar win-count differences (one or two draws) are
reported rather than called exact reproduction. No production reconciliation
or full goalie pipeline rerun this turn; their implementation is unchanged.

New scorecard includes 4,632 production-adapter fallback season rows. On the
35,878 answerable rows, A RMSE .8640 vs .9129 and wins 2,000/2,000. Both samples
must be reported. Scorecard also misstates historically inspected 2022-2025
pages as sealed, overstates pooled calibration, calls RMSE squared error, and
claims every metric favors A despite B's slightly better dollar MAE. Record
these as scorecard corrections, not another forecast-development cycle.

Player Model candidate acceptance closed with known limitations; adoption is
deferred to the user's later Picks/Prospects decision. New review and runnable
audit helper in 50_REBUILD; project state, queue, flags and decision log updated.
No production/model default changes, candidate merge, or vendor writes.

The fallback audit also finds 65 of 1,176 scored contracts lack production's
anchor. On 1,111 answerable contracts with the existing primary line, A point
RMSE 3.614M vs 3.648M, MAE 1.761M vs 1.854M; squared-error wins 1,245/2,000,
absolute-error wins 1,934/2,000. This is a scoring subset, not a refitted line.

---

## 2026-09-24l: scorecard corrected; candidate closed; decision deferred

- Review `57d0be8` (merged as `4f40966`): the Player Model candidate closes at Phase 5 with its
  recorded limitations; adoption deferred until picks and prospects resume; "close the candidate,
  correct the scorecard, and move on". No further forecast experiments.
- Read `production_adapter.predict`: where production has no anchor it carries the harness's
  trailing total flat and tags `outside_production`. The scorecard had called all of those rows
  production. `run_model_scorecard.py` v1.1 adds the answerable sample (same rows for all three
  models) for season forecasts and, via production's own anchor rule at each signing page, for
  contract dollars.
- Rerun: full-sample figures unchanged. Answerable season rows 35,878: RMSE 0.9129 / 0.8640 / 0.8645
  (current / adopted / previous), candidates lower in 2000/2000, lower at every horizon (9.0% at the
  valuation season to 1.8% five out). Star bias crosses a season earlier on these rows (two out:
  adopted −0.64, current +0.62). Answerable contracts 1,111: RMSE $3.648M / $3.614M / $3.630M, adopted
  lower 1246/2000 squared, 1934/2000 absolute. Reading unchanged.
- `Model_Scorecard.md` corrected: 2022-2025 inspected by about thirty variants (plan decision D), not
  sealed; pooled calibration is not per-contract; 6.1% is RMSE (11.9% in mean squared error); B has
  the slightly lower dollar absolute error (1,056/2,000 point, 2,000/2,000 simulated); the
  reconciliation does not split term pricing from forecasting; current's chance of playing is the
  survival weight, one in the valuation season. Recommendation narrowed and marked for the later
  decision.
- CLAUDE.md: two rules (a comparator's fallback rows; sealed holdout / pooled calibration / naming
  the metric).

Files: `run_model_scorecard.py` v1.1, `Model_Scorecard.md`, `CLAUDE.md`, the four state files.

## 2026-09-24m: decision brief for the skater model choice

- Thomas asked for a short document explaining the current model's issues and what the rebuild does
  about them, with a flowchart of both chains' steps, to support the deferred model decision.
- Written from the code, not from summaries: `20_CODE/skater_forward_projection.py` (raw 60/40
  anchor; ratio path renormalised so the first season keeps the raw total; four guards; negative
  anchor to replacement; backcast spread flat past three seasons), `aging_curve.py`, `exit_hazard.py`,
  `anchor_shrink_test.rate_sample` (start-year dating), and the rebuild's `ability_forecast.py`,
  `aging_additive.py`, `participation_model.py`, `production_currency.py`, `run_npv_simulation.py`.
- New `50_REBUILD/docs/Skater_Model_Decision.html`: seven-step side-by-side flowchart, a card per
  step (issue and replacement), the corrected scores on production's answerable sample, the
  limitations, and the three options. Published as a private artifact. No model or state change.
